<?xml version="1.0" encoding="utf-8"?>
<rss version="2.0" xmlns:sparkle="http://www.andymatuschak.org/xml-namespaces/sparkle" xmlns:dc="http://purl.org/dc/elements/1.1/">
  <channel>
    <title>Droidective</title>
    <link>https://droidective.com/appcast.xml</link>
    <description>Most recent Droidective updates.</description>
    <language>en</language>
    <item>
      <title>Droidective 3.12.0</title>
      <sparkle:version>956</sparkle:version>
      <sparkle:shortVersionString>3.12.0</sparkle:shortVersionString>
      <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
      <description><![CDATA[
<style>
:root { color-scheme: light dark; }
body { font: 13px/1.55 -apple-system, BlinkMacSystemFont, sans-serif; margin: 0; }
h2 { font-size: 1.3em; margin: .2em 0 .4em; }
h3 { font-size: 1.05em; margin: 1.1em 0 .3em; }
p { margin: .5em 0; }
ul { margin: .4em 0; padding-left: 1.4em; }
li { margin: .3em 0; }
code { font-family: ui-monospace, SFMono-Regular, Menlo, monospace; font-size: .9em; background: color-mix(in srgb, currentColor 12%, transparent); padding: .1em .35em; border-radius: 4px; }
pre { background: color-mix(in srgb, currentColor 8%, transparent); padding: .6em .8em; border-radius: 6px; overflow-x: auto; }
pre code { background: none; padding: 0; }
a { color: #2f9e44; }
</style>
<p>Any feature can now have a window of its own — drag its tab out, or pick it from
a menu — and the work inside it carries on where it was instead of starting
over.</p>
<h3>Any tab can become its own window</h3>
<ul>
<li>
<strong>Drag a tab out of the app and it becomes a window.</strong> Drop it on the desktop,
on another display, beside your editor. Drop it on another Droidective window
instead and it moves in there.</li>
<li>
<strong>Or use a menu</strong>: right-click a tab for "Open in New Window" or "Move to
Window ▸", or Tab ▸ Move Tab to New Window (⌃⌘N) for the one you are on.</li>
<li>
<strong>A torn-off window keeps the device its tab was pointed at.</strong> Every other way
of opening a window deliberately picks a device no other window holds; for a
tab you have just moved that would quietly swap the device out from under it.</li>
<li>
<strong>A window's last tab can join another window</strong> — consolidating two windows
into one is a reasonable thing to want — but it cannot spawn a third and leave
its own window empty.</li>
<li>
<strong>Escape cancels a drag</strong>, and a tab released on nothing snaps back.</li>
</ul>
<h3>A moved tab keeps working</h3>
<p>Moving a tab rebuilds its view, which used to mean starting the feature again.
Now the work moves with it:</p>
<ul>
<li>
<strong>The Terminal takes its shells.</strong> Running processes keep running, scrollback
is intact, split panes and their directories come along.</li>
<li>
<strong>A screen recording keeps recording.</strong> Start it in one window, move the tab,
stop it in another, and the take is one continuous file — no gap, no split.
The same for a recording made from the mirror.</li>
<li>
<strong>A live mirror keeps streaming.</strong> No "Connecting…", no black second while the
device re-primes — the same session, the same encoder, the same tunnel. The
Mirror Wall moves all six at once.</li>
<li>
<strong>Unsaved screenshot markup survives</strong>, undo history and all — nothing in that
editor is written to disk until you save or copy it, so a move used to be the
one loss you could not undo. Moving no longer asks about it; closing the tab
still does.</li>
<li>
<strong>A decompiled APK stays decompiled.</strong> jadx and apktool take minutes on a real
app, and a move used to run them again from the top.</li>
<li>
<strong>The video editor keeps its edits and its player</strong>, including the transcoded
copy it builds for formats AVFoundation cannot play, which for a long
recording is a transcode of the whole file.</li>
<li>Also carried across: Reactotron's timeline, the JS Console's session, APK
Studio's loaded APK, Logcat and iOS Logs buffers, collected crashes, API
Testing's request, the folder you had browsed to, the Apps list and what you
had selected in it, and anything half-typed in Send Text, Deep Links or Custom
Commands.</li>
<li>
<strong>Performance and Network Speed deliberately do not move.</strong> Their sampler
stops with the view, so a preserved graph would have a silent gap in it —
worse than being told. They ask before you leave, as they did.</li>
</ul>
<h3>Fixes</h3>
<ul>
<li>
<strong>Dragging a tab to the empty part of its own tab strip works.</strong> It used to do
nothing at all — the tab stayed exactly where it was.</li>
</ul>
<h3>Known gaps</h3>
<ul>
<li>A moved tab starts at the top of whatever it is showing: the list, tree or
buffer crosses, the scroll position does not.</li>
<li>A decompile or a video transcode still in progress restarts when its tab
moves. Once finished, it stays.</li>
</ul>
]]></description>
      <pubDate>Sun, 06 Sep 2026 19:05:44 +0000</pubDate>
      <enclosure url="https://github.com/Droidective/Droidective/releases/download/v3.12.0/Droidective-v3.12.0.dmg" sparkle:edSignature="a/8tB+0Fv+MAVLPEd+S8+x+p3WV1bRUqMhqWU86qQ83G3lfL84pLaS1xnNTdDws/081KV0zUG0m6j0kMadc8Cw==" length="146242418" type="application/octet-stream" />
    </item>
    
  </channel>
</rss>
