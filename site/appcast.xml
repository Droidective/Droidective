<?xml version="1.0" encoding="utf-8"?>
<rss version="2.0" xmlns:sparkle="http://www.andymatuschak.org/xml-namespaces/sparkle" xmlns:dc="http://purl.org/dc/elements/1.1/">
  <channel>
    <title>Droidective</title>
    <link>https://droidective.com/appcast.xml</link>
    <description>Most recent Droidective updates.</description>
    <language>en</language>
    <item>
      <title>Droidective 3.12.1</title>
      <sparkle:version>970</sparkle:version>
      <sparkle:shortVersionString>3.12.1</sparkle:shortVersionString>
      <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
      <description><![CDATA[
<style>
:root { color-scheme: light dark; }
body { font: 13px/1.55 -apple-system, BlinkMacSystemFont, sans-serif; margin: 0; }
h2 { font-size: 1.3em; margin: .2em 0 .4em; }
h3 { font-size: 1.05em; margin: 1.1em 0 .3em; }
p { margin: .5em 0; }
ul { margin: .4em 0; padding-left: 1.4em; }
li { margin: .3em 0; }
code { font-family: ui-monospace, SFMono-Regular, Menlo, monospace; font-size: .9em; background: color-mix(in srgb, currentColor 12%, transparent); padding: .1em .35em; border-radius: 4px; }
pre { background: color-mix(in srgb, currentColor 8%, transparent); padding: .6em .8em; border-radius: 6px; overflow-x: auto; }
pre code { background: none; padding: 0; }
a { color: #2f9e44; }
</style>
<p>Pinned tabs, Send Text's snippets in the Quick Actions panel, and the fix for
the thing that made a long Reactotron or JS Console session turn into an app
that stopped responding for seconds at a time.</p>
<h3>Pin a tab</h3>
<ul>
<li>
<strong>Right-click a tab ▸ Pin Tab</strong> (or Tab ▸ Pin Tab). Pinned tabs sit at the
front of their pane and stay there, and they survive "Close Other Tabs".</li>
<li>The × is replaced by a pin you can click to unpin, so the tabs you keep open
all day are out of reach of a stray click. It is not a lock: ⌘W and Close Tab
still close a pinned tab, the way they do in Chrome and VS Code.</li>
<li>A pinned tab stays pinned when you move it to the other pane or drag it out
into a window of its own.</li>
</ul>
<h3>Snippets in the Quick Actions panel</h3>
<ul>
<li>
<strong>Send Text's saved snippets now appear in the panel</strong>, under the text field —
the same recency-ranked list the full Send Text screen shows, with the top few
and the rest behind "Show more".</li>
<li>↓/↑ walk them and ⏎ inserts the highlighted one; ⌘⏎ still runs. <code>{clipboard}</code>
and <code>{ip}</code> fill in as they do everywhere else, and using one from the panel
bumps it up the list in the main window too.</li>
</ul>
<h3>Feeds no longer hold a gigabyte each</h3>
<ul>
<li>
<strong>The Reactotron timeline and the JS Console now size their buffers to your
Mac</strong> instead of to a fixed number. Both capped 128 MB of received data, but a
decoded log row costs several times what it did on the wire, so the real cost
was closer to a gigabyte — each. One session was measured climbing from 363 MB
to 1.58 GB over three hours and then staying there for a day.</li>
<li>Nothing was leaking. The buffers were evicting correctly; the limit itself was
simply far too generous. On a 16 GB Mac each feed now holds up to 192 MB.</li>
</ul>
<h3>A feed you cannot see stops rendering</h3>
<ul>
<li>
<strong>A feed in a covered window no longer re-renders every second.</strong> Whether the
app was "visible" was inferred from which tab was in front and whether
Droidective was the active app, and neither answers the actual question. With
the app behind another window and a Reactotron tab in front, the timeline kept
redrawing for as long as it was left there.</li>
<li>It now asks the window server directly. Events keep arriving the whole time —
only the drawing waits — and bringing the window back shows everything at
once.</li>
</ul>
<h3>Hangs can now be measured</h3>
<ul>
<li>
<strong>A hang report says how long the hang was.</strong> It never could before: the
duration came from the detection threshold, so every report read "at least
2 seconds" whether the app was gone for two seconds or forty.</li>
<li>Droidective now also records what each feed was holding when the app
struggled, which is what makes this kind of problem findable instead of
guessable. Numbers only — no log contents, no URLs, no device identifiers, and
it respects the Settings ▸ Privacy toggles like everything else.</li>
</ul>
]]></description>
      <pubDate>Wed, 09 Sep 2026 06:33:21 +0000</pubDate>
      <enclosure url="https://github.com/Droidective/Droidective/releases/download/v3.12.1/Droidective-v3.12.1.dmg" sparkle:edSignature="1OTC3FClak6uCwCqNnoAC8w7RCj+VAyTTczpIxgfgkwgEg97frTyZXOM9KTn6fyhqiLzSQyK5+BiSbXumJeGAw==" length="146225754" type="application/octet-stream" />
    </item>
    
  </channel>
</rss>
