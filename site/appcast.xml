<?xml version="1.0" encoding="utf-8"?>
<rss version="2.0" xmlns:sparkle="http://www.andymatuschak.org/xml-namespaces/sparkle" xmlns:dc="http://purl.org/dc/elements/1.1/">
  <channel>
    <title>Droidective</title>
    <link>https://droidective.com/appcast.xml</link>
    <description>Most recent Droidective updates.</description>
    <language>en</language>
    <item>
      <title>Droidective 3.0.1</title>
      <sparkle:version>376</sparkle:version>
      <sparkle:shortVersionString>3.0.1</sparkle:shortVersionString>
      <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
      <description><![CDATA[
<style>
:root { color-scheme: light dark; }
body { font: 13px/1.55 -apple-system, BlinkMacSystemFont, sans-serif; margin: 0; }
h2 { font-size: 1.3em; margin: .2em 0 .4em; }
h3 { font-size: 1.05em; margin: 1.1em 0 .3em; }
p { margin: .5em 0; }
ul { margin: .4em 0; padding-left: 1.4em; }
li { margin: .3em 0; }
code { font-family: ui-monospace, SFMono-Regular, Menlo, monospace; font-size: .9em; background: color-mix(in srgb, currentColor 12%, transparent); padding: .1em .35em; border-radius: 4px; }
pre { background: color-mix(in srgb, currentColor 8%, transparent); padding: .6em .8em; border-radius: 6px; overflow-x: auto; }
pre code { background: none; padding: 0; }
a { color: #2f9e44; }
</style>
<p>A bug-fix release: confirmations before destructive actions, several features
that now work without an Android device selected, honest status messages, and
tighter analytics.</p>
<h3>Fixes</h3>
<ul>
<li>
<strong>Uninstall asks first</strong> — the Uninstall button in the Apps list now shows a
confirmation, matching Clear Data. It previously removed the app and its data
on the first click.</li>
<li>
<strong>Logcat streams after a device authorizes</strong> — opening Logcat on a device
that was still unauthorized left the stream empty even after you approved the
adb prompt. It now starts as soon as the device is ready.</li>
<li>
<strong>APK tools open without a device</strong> — APK Studio, APK Inspector, Sign APK, and
Decompile APK work on local files and no longer require an Android device;
they stayed usable with only an iOS Simulator selected.</li>
<li>
<strong>Monkey Test confirms before running</strong> — it warns before sending random
events, and when a long run hits the time limit it reports that events were
already sent instead of "failed".</li>
<li>
<strong>Frida on a non-rooted device</strong> — Frida now checks for root before pushing
the server and shows a clear message pointing to frida-gadget, instead of a
status that said "Started" while also requiring root.</li>
<li>
<strong>Change Locale is honest</strong> — the toast now says the locale change was
requested (a full system change can require root) rather than claiming it
was set.</li>
<li>
<strong>Crash Catcher shows one crash</strong> — the last-crash view no longer runs
together multiple crashes when several are buffered.</li>
<li>
<strong>Reactotron opens without an Android device</strong> — its server is host-side, so
it no longer requires an Android device to be selected.</li>
<li>
<strong>Screen Record surfaces a disconnect</strong> — if the device drops mid-recording,
the timer stops and the captured footage is kept for saving, instead of the
timer running on against a dead stream.</li>
<li>
<strong>Performance recording survives a disconnect</strong> — samples are kept and stay
exportable when the device disconnects, rather than being cleared.</li>
<li>
<strong>Network Speed totals</strong> — session download/upload totals no longer jump to a
bogus value if the device reboots mid-recording.</li>
<li>
<strong>Quick Actions panel</strong> — "all devices" only fans out actions that support it
(matching the main window), multi-device runs report each device's outcome,
the panel opens on the display under the pointer, and a quick screenshot names
the saved file.</li>
<li>Manage App's confirmation states how many devices it affects; the app picker
refreshes when you switch devices; and tool detection re-checks after you
install adb (or another tool) while the app is open.</li>
</ul>
<h3>Privacy</h3>
<ul>
<li>Anonymous analytics no longer include the device model or Android version —
only whether the device is an emulator or wireless. This matches the privacy
policy and the Settings description.</li>
</ul>
<h3>Improvements</h3>
<ul>
<li>
<strong>Bundled developer tools are pinned</strong> — jadx, apktool, uber-apk-signer,
frida-server, and the bundled Java runtime install known-good pinned versions
instead of whatever is latest upstream.</li>
<li>Third-party license notices now cover every bundled and linked component.</li>
</ul>
<p>Installed copies update in place via Sparkle.</p>
]]></description>
      <pubDate>Sun, 12 Jul 2026 10:24:10 +0000</pubDate>
      <enclosure url="https://github.com/Droidective/Droidective/releases/download/v3.0.1/Droidective-v3.0.1.dmg" sparkle:edSignature="enjLXG48itzLg8s8k6laYbaxhDXvgBt/84jJUKzjd0nBQ/ME0ujb2REXMQ16ffzdSwUWopY3C8tz9UH9J/5fAQ==" length="51320857" type="application/octet-stream" />
    </item>
  </channel>
</rss>
