<?xml version="1.0" encoding="utf-8"?>
<rss version="2.0" xmlns:sparkle="http://www.andymatuschak.org/xml-namespaces/sparkle" xmlns:dc="http://purl.org/dc/elements/1.1/">
  <channel>
    <title>Droidective</title>
    <link>https://droidective.com/appcast.xml</link>
    <description>Most recent Droidective updates.</description>
    <language>en</language>
    <item>
      <title>Droidective 2.9.3</title>
      <sparkle:version>340</sparkle:version>
      <sparkle:shortVersionString>2.9.3</sparkle:shortVersionString>
      <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
      <description><![CDATA[
<style>
:root { color-scheme: light dark; }
body { font: 13px/1.55 -apple-system, BlinkMacSystemFont, sans-serif; margin: 0; }
h2 { font-size: 1.3em; margin: .2em 0 .4em; }
h3 { font-size: 1.05em; margin: 1.1em 0 .3em; }
p { margin: .5em 0; }
ul { margin: .4em 0; padding-left: 1.4em; }
li { margin: .3em 0; }
code { font-family: ui-monospace, SFMono-Regular, Menlo, monospace; font-size: .9em; background: color-mix(in srgb, currentColor 12%, transparent); padding: .1em .35em; border-radius: 4px; }
pre { background: color-mix(in srgb, currentColor 8%, transparent); padding: .6em .8em; border-radius: 6px; overflow-x: auto; }
pre code { background: none; padding: 0; }
a { color: #2f9e44; }
</style>
<p>The streaming log feeds — Logcat, JS Console, and the Reactotron timeline —
rebuilt around one scroll behavior, plus JS-console connection fixes and
Reactotron network filters.</p>
<h3>New features</h3>
<ul>
<li>
<strong>Jump to top / bottom in every log feed</strong> — floating buttons overlay Logcat,
the JS Console, and the Reactotron timeline; each hides while you're at its
edge, and both hide when there's nothing to scroll. Jumping to the newest
edge resumes live following.</li>
<li>
<strong>Reactotron network filters</strong> — pick the Network view in a timeline pane and
filter by HTTP method (the ones your app actually sent) and status class
(2xx–5xx, plus Failed for requests that never got a response) — per pane, so
a split view can watch two slices.</li>
<li>
<strong>Reactotron explains disconnects</strong> — when a client drops, an expandable
DISCONNECTED row lands in the timeline where streaming stopped, naming the
app and the reason — including the Android case where the app queues events
faster than the connection drains and its WebSocket hangs up at a 16 MB
backlog.</li>
</ul>
<h3>Improvements</h3>
<ul>
<li>
<strong>Interruption-free reading while streaming</strong> — scroll away from the newest
line and new logs keep rendering without dragging your view; return to the
edge (or tap the jump button) and following resumes. The reverse-order
toggles are gone: JS Console and Logcat tail at the bottom, Reactotron keeps
newest at top.</li>
<li>
<strong>Steady under event storms</strong> — the feeds no longer re-render per scrolled
pixel or per streamed event, which could peg the CPU and hang the app under
a fast Reactotron stream.</li>
<li>The connected app's name shows next to Reactotron's status dot.</li>
</ul>
<h3>Fixes</h3>
<ul>
<li>
<strong>JS console no longer loops "connecting…"</strong> — apps logging large objects
tripped a 1 MiB socket cap on the post-connect replay (close code 1009), and
every reconnect replayed the same burst. The cap is now 64 MiB.</li>
<li>
<strong>Consoles stop with their UI</strong> — closing the JS console or Reactotron tab,
or the main window (in either background-mode setting), tears the connection
down instead of leaving it reconnecting in the background; reopening the
window resumes the JS console's discovery.</li>
</ul>
<p>Installed copies update in place via Sparkle.</p>
]]></description>
      <pubDate>Thu, 09 Jul 2026 11:37:35 +0000</pubDate>
      <enclosure url="https://github.com/Droidective/Droidective/releases/download/v2.9.3/Droidective-v2.9.3.dmg" sparkle:edSignature="rItZEN4H6AUaGqjaxeax5tlxNj/zQq8HkFthbe2kHgfw5EMlAWYfwJo6qkiH4KH86IxPh1M7zYIdIbRcL9dhCA==" length="46008163" type="application/octet-stream" />
    </item>
  </channel>
</rss>
