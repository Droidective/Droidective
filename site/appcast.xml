<?xml version="1.0" encoding="utf-8"?>
<rss version="2.0" xmlns:sparkle="http://www.andymatuschak.org/xml-namespaces/sparkle" xmlns:dc="http://purl.org/dc/elements/1.1/">
  <channel>
    <title>Droidective</title>
    <link>https://droidective.com/appcast.xml</link>
    <description>Most recent Droidective updates.</description>
    <language>en</language>
    <item>
      <title>Droidective 3.11.0</title>
      <sparkle:version>941</sparkle:version>
      <sparkle:shortVersionString>3.11.0</sparkle:shortVersionString>
      <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
      <description><![CDATA[
<style>
:root { color-scheme: light dark; }
body { font: 13px/1.55 -apple-system, BlinkMacSystemFont, sans-serif; margin: 0; }
h2 { font-size: 1.3em; margin: .2em 0 .4em; }
h3 { font-size: 1.05em; margin: 1.1em 0 .3em; }
p { margin: .5em 0; }
ul { margin: .4em 0; padding-left: 1.4em; }
li { margin: .3em 0; }
code { font-family: ui-monospace, SFMono-Regular, Menlo, monospace; font-size: .9em; background: color-mix(in srgb, currentColor 12%, transparent); padding: .1em .35em; border-radius: 4px; }
pre { background: color-mix(in srgb, currentColor 8%, transparent); padding: .6em .8em; border-radius: 6px; overflow-x: auto; }
pre code { background: none; padding: 0; }
a { color: #2f9e44; }
</style>
<p>Two new ways to get things onto a device — drop a file on the mirror, or pair
by showing the phone a QR code — and streaming feeds that cost half as much
while nobody is looking at them.</p>
<h3>Drop files onto a mirror</h3>
<ul>
<li>
<strong>Drag an app package onto the mirror and it installs on that device.</strong>
Anything else — a photo, a PDF, a folder — copies to <code>/sdcard/Download</code> and
is handed to the device's media library, so a dropped screenshot turns up in
the Gallery rather than only in Files.</li>
<li>
<strong>The mirror says what will happen before you let go.</strong> "Install on Pixel 7",
"Copy 3 files to Pixel 7", with the destination named. A drop that replaces
an app or writes to storage should not be a surprise.</li>
<li>
<strong>Every mirror takes drops</strong> — the Mirror Screen tab, a popped-out window,
and each Mirror Wall tile. A tile is a device, not a picture of one: a file
dropped on the third tile goes to the third device whatever the device bar
is pointed at, and six tiles take six independent transfers.</li>
<li>
<strong>Installing asks first, and says what it is replacing.</strong> The prompt reads
the package and the device and shows the version it is moving from and to,
with the button named for what will actually happen — Install, Update,
Reinstall, or Downgrade &amp; Replace. A downgrade says up front that app data
cannot be kept, because Android will not do one in place.</li>
<li>
<strong>A failed install offers the fix.</strong> A package signed with a different key
cannot install over the copy already on the device; that failure now carries
an Uninstall &amp; Install button instead of a message you have to act on
yourself. Failures an uninstall would not fix — a full disk, the wrong CPU —
do not offer it.</li>
<li>
<strong>Copies report a real percentage and can be stopped.</strong> A large file shows
how far it has got rather than a spinner, and cancelling removes the
half-written file from the device.</li>
</ul>
<h3>Nothing dropped on the app is ignored</h3>
<ul>
<li>
<strong>A file dropped on a screen that has nothing to do with it now lands
somewhere useful.</strong> An app package opens the package screen, a video opens
the Video Editor, anything else copies to the selected device — and the
window says which before you let go. An APK dropped on Reactotron used to do
nothing at all, with nothing on screen saying why.</li>
<li>
<strong>Files opened from Finder take the same route</strong>, so double-clicking a file
and dragging one in can no longer disagree about where it belongs.</li>
</ul>
<h3>Pair over Wi-Fi by scanning a QR code</h3>
<ul>
<li>
<strong>Show a code, point the phone at it, done.</strong> Settings ▸ Developer options ▸
Wireless debugging ▸ Pair device with QR code reads it, and the pairing
finishes with nothing typed on either side.</li>
<li>
<strong>It removes the two things that go wrong on the typed path</strong>: the pairing
port, which is random per session and is <em>not</em> the port on the Wireless
debugging screen, and a six-digit code that expires while you are typing it.</li>
<li>
<strong>The device dropdown's "Pair new device…" now opens this.</strong> The typed-code
path sits beside it as "Pair with a code…", unchanged.</li>
</ul>
<h3>Feeds cost less while you are not watching</h3>
<ul>
<li>
<strong>Logcat, the JS Console, the Reactotron timeline and iOS Logs pace
themselves</strong> by whether anyone can see them and by how busy the app already
is: four times a second on screen and in front, once a second on screen
behind another app, and every five seconds while hidden behind another tab.</li>
<li>
<strong>Hidden is a pace, not a pause.</strong> Nothing is dropped, and returning to a
feed flushes it at once, so the longer interval is never what you wait on.</li>
<li>
<strong>A feed left open while you work elsewhere costs about half what it did.</strong>
With a device streaming 300 log lines a second, a hidden tab measured 17.7%
CPU in v3.10.0 and 8.7% here. Nearly every report of the app becoming
unresponsive came from that state — a feed running at full speed for a window
nobody was looking at.</li>
</ul>
<h3>Fixes</h3>
<ul>
<li>
<strong>The pairing code no longer appears in the Command Log.</strong> It is a
credential for adb access to your device, and the log is what gets copied
out of Settings into a bug report. The endpoint is kept; the code is masked.</li>
<li>
<strong>The mirror's own window is easier to find.</strong> It has existed since v2.9.2,
behind the last button on the mirror's control bar — which folds into an
overflow menu as soon as the pane is narrow. It is now Window ▸ Open Mirror
in New Window (⌃⌘M), and a row in the device dropdown.</li>
</ul>
]]></description>
      <pubDate>Sun, 06 Sep 2026 10:45:27 +0000</pubDate>
      <enclosure url="https://github.com/Droidective/Droidective/releases/download/v3.11.0/Droidective-v3.11.0.dmg" sparkle:edSignature="lILfGsEtuBEVhOyDoIbcQ35847L7QYEaPc7jLwBuoT+7t9tSY5j+isjK51PDYv1Egx46EYf2SFd1JNbsQWDGBw==" length="145869781" type="application/octet-stream" />
    </item>
    
  </channel>
</rss>
