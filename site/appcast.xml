<?xml version="1.0" encoding="utf-8"?>
<rss version="2.0" xmlns:sparkle="http://www.andymatuschak.org/xml-namespaces/sparkle" xmlns:dc="http://purl.org/dc/elements/1.1/">
  <channel>
    <title>Droidective</title>
    <link>https://droidective.com/appcast.xml</link>
    <description>Most recent Droidective updates.</description>
    <language>en</language>
    <item>
      <title>Droidective 3.8.0</title>
      <sparkle:version>702</sparkle:version>
      <sparkle:shortVersionString>3.8.0</sparkle:shortVersionString>
      <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
      <description><![CDATA[
<style>
:root { color-scheme: light dark; }
body { font: 13px/1.55 -apple-system, BlinkMacSystemFont, sans-serif; margin: 0; }
h2 { font-size: 1.3em; margin: .2em 0 .4em; }
h3 { font-size: 1.05em; margin: 1.1em 0 .3em; }
p { margin: .5em 0; }
ul { margin: .4em 0; padding-left: 1.4em; }
li { margin: .3em 0; }
code { font-family: ui-monospace, SFMono-Regular, Menlo, monospace; font-size: .9em; background: color-mix(in srgb, currentColor 12%, transparent); padding: .1em .35em; border-radius: 4px; }
pre { background: color-mix(in srgb, currentColor 8%, transparent); padding: .6em .8em; border-radius: 6px; overflow-x: auto; }
pre code { background: none; padding: 0; }
a { color: #2f9e44; }
</style>
<p>API Testing arrives as the 60th tool — a full HTTP client with Postman import
and export, collections, environments, assertions, and code generation.
Recordings can now capture microphone audio alongside the device, the Terminal
scrolls properly in full-screen and streaming programs, and four main-thread
hangs reported from v3.7.1 are fixed.</p>
<h3>API Testing — the 60th feature</h3>
<ul>
<li>
<strong>A real HTTP client, in the palette</strong> — all seven methods, six body kinds
(JSON, form-urlencoded, multipart, raw, GraphQL, binary), and five auth
kinds (bearer, basic, API key in header or query, OAuth 2). It needs no
device, so it works with nothing plugged in.</li>
<li>
<strong>Postman import and export</strong> — bring collections and environments across,
and send them back. Import and export failures now report a reason instead
of failing silently.</li>
<li>
<strong>Collections, folders, and environments</strong> — a searchable sidebar with
nested folders and per-item move, duplicate, and delete; <code>{{variables}}</code>
resolve from environment and global scopes, and unresolved ones are flagged
before a request goes out rather than being sent literally.</li>
<li>
<strong>Assertions and a collection runner</strong> — assert on status code, response
time, body size, body text, any header, or a JSON path, and run a whole
collection in one sheet.</li>
<li>
<strong>Generated code</strong> — every request exports as cURL, HTTPie, <code>fetch</code>, axios,
Python <code>requests</code>, or Swift <code>URLSession</code>. Pasting a cURL command parses it
back into a request, including <code>-u</code> credentials.</li>
<li>
<strong>A response pane that shows the response</strong> — pretty and raw body, image
preview, headers, cookies, a timing breakdown, the redirect chain, and
save-to-file. JSON keeps the server's key order; the earlier printer sorted
keys, which misrepresents the payload you are inspecting.</li>
</ul>
<h3>Screen recording</h3>
<ul>
<li>
<strong>Microphone audio</strong> — a recording can capture the device's audio, the Mac's
microphone, both, or neither. The device side is playback <em>or</em> the device's
own microphone, never both — scrcpy carries one device stream per session —
so those two options are mutually exclusive and say so.</li>
<li>
<strong>Set it up before you hit record</strong> — the chevron beside the mirror's record
button opens a sheet with the three controls and a live level meter; the
Screen Record screen shows the same controls inline. Mid-take, each source
gets a mute control.</li>
<li>
<strong>One audio track, not two</strong> — both sources are mixed onto a single track,
because players (QuickTime, browsers, chat apps) play only the first audio
track and silently drop the rest. Muting writes silence rather than dropping
samples, so the timeline stays continuous and unmuting is instant.</li>
</ul>
<h3>Terminal</h3>
<ul>
<li>
<strong>The wheel now scrolls full-screen programs</strong> — an agent CLI, <code>htop</code>,
<code>less --mouse</code>, or vim with <code>mouse=a</code> could not be scrolled at all: those
programs take the mouse over and run on the alternate screen, where there is
no viewport to move. The wheel is now reported to the program, or sent as
cursor keys when the program hasn't taken the mouse (xterm alternate
scroll), following the scroll's own distance rather than flinging a pager
pages at a time.</li>
<li>
<strong>Scrolling one no longer corrupts the screen</strong> — the alternate screen's
right margin was left at 0, so a scroll region copied a single column per
row and left the rest stale, leaving two frames interleaved on screen. The
margin is repaired when the alternate screen activates, leaving a program's
own margins alone.</li>
<li>
<strong>Scrollback stays put while output streams</strong> — scrolling back through a
running command snapped to the newest line on every line written. The view
now holds where you left it; typing returns to the live output.</li>
</ul>
<h3>Fixes</h3>
<ul>
<li>
<strong>Launch hang on a cold font registry</strong> — enumerating installed fonts ran on
the main thread during launch and could block past the 2-second hang
threshold. It now runs off the main actor through Core Text (13 ms against
the previous 91 ms warm, same 245 families).</li>
<li>
<strong>Log tail stalls</strong> — scrolling the log to the bottom forced the text system
to typeset the entire buffer, so a filter change, an order flip, or a large
append could freeze the app. The view scrolls to the document height
instead, with non-contiguous layout enabled.</li>
<li>
<strong>Opening Settings ▸ General stalled</strong> — it read the launch-at-login status
with a synchronous launchd round-trip on the main thread.</li>
<li>
<strong>Beachball when resizing with a heavyweight tab open</strong> — every mounted tab
re-laid out on each tick of a divider drag, including hidden ones; a Crash
Catcher trace of a few thousand lines turned that into a multi-second hang.
Hidden tabs now pin to their last resting size mid-resize and re-wrap once
at rest.</li>
<li>
<strong>ffmpeg errors were hidden by its own progress output</strong> — ffmpeg separates
progress updates with carriage returns, so the export and segment-stitching
error dialogs showed the progress blob instead of the actual error. Both now
split on all newline forms.</li>
<li>
<strong>Stale results from cancelled loads</strong> — navigating away from a view while it
was still fetching could write the old result into the new state.</li>
</ul>
<h3>Under the hood</h3>
<ul>
<li>
<strong>Windows and Linux groundwork</strong> — ADBKit, the layer holding all of
Droidective's logic, now compiles and runs its test suite on Linux and
Windows as well as macOS, with a guard test that fails the build on new
Apple-only code outside an explicit gate. Nothing changes for macOS users;
this is the foundation for the Windows and Linux builds, which will ship on
the beta channel (<code>docs/release-channels.md</code>).</li>
</ul>
]]></description>
      <pubDate>Sat, 01 Aug 2026 23:30:34 +0000</pubDate>
      <enclosure url="https://github.com/Droidective/Droidective/releases/download/v3.8.0/Droidective-v3.8.0.dmg" sparkle:edSignature="mzh6ceqnVGgDVRSu/5coASNBhR8Mb9uFDxuQoFFb+tyuVk6KJSSfzY6zA36JbQEjWNlKD0+1XUJcars7FSitAw==" length="143684841" type="application/octet-stream" />
    </item>
    
  </channel>
</rss>
