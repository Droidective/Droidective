<?xml version="1.0" encoding="utf-8"?>
<rss version="2.0" xmlns:sparkle="http://www.andymatuschak.org/xml-namespaces/sparkle" xmlns:dc="http://purl.org/dc/elements/1.1/">
  <channel>
    <title>Droidective</title>
    <link>https://droidective.com/appcast.xml</link>
    <description>Most recent Droidective updates.</description>
    <language>en</language>
    <item>
      <title>Droidective 2.8.2</title>
      <sparkle:version>254</sparkle:version>
      <sparkle:shortVersionString>2.8.2</sparkle:shortVersionString>
      <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
      <description><![CDATA[
<style>
:root { color-scheme: light dark; }
body { font: 13px/1.55 -apple-system, BlinkMacSystemFont, sans-serif; margin: 0; }
h2 { font-size: 1.3em; margin: .2em 0 .4em; }
h3 { font-size: 1.05em; margin: 1.1em 0 .3em; }
p { margin: .5em 0; }
ul { margin: .4em 0; padding-left: 1.4em; }
li { margin: .3em 0; }
code { font-family: ui-monospace, SFMono-Regular, Menlo, monospace; font-size: .9em; background: color-mix(in srgb, currentColor 12%, transparent); padding: .1em .35em; border-radius: 4px; }
pre { background: color-mix(in srgb, currentColor 8%, transparent); padding: .6em .8em; border-radius: 6px; overflow-x: auto; }
pre code { background: none; padding: 0; }
a { color: #2f9e44; }
</style>
<p>A patch release that fixes the JS Console's reconnect behavior against Metro
and adds anonymous app performance monitoring.</p>
<h3>Fixes</h3>
<ul>
<li>
<strong>JS Console reconnect storm</strong> — a race in the CDP client killed each new
connection moments after it opened and left the old socket running, so the
console reconnected every couple of seconds, Metro logged a stream of
connection-established messages, and leaked debugger connections piled up
until a JS reload cleared them. Connections are now generation-guarded, every
close path shuts its socket, and the handshake times out instead of hanging
on a dead target — the console connects once and stays connected.</li>
<li>Changing the Metro port can no longer commit a connection that was still in
flight against the old port.</li>
</ul>
<h3>Improvements</h3>
<ul>
<li>
<strong>Performance self-monitoring</strong> — the app now watches its own CPU and memory
use and, when something stays over the limit, reports it to the anonymous
diagnostics along with which features were open — so resource hogs get found
and fixed. Sends only feature names and resource numbers; opt-out any time in
Settings → Privacy.</li>
</ul>
<p>Installed copies update in place via Sparkle.</p>
]]></description>
      <pubDate>Fri, 03 Jul 2026 10:58:48 +0000</pubDate>
      <enclosure url="https://github.com/Droidective/Droidective/releases/download/v2.8.2/Droidective-v2.8.2.dmg" sparkle:edSignature="BcAE66yva5o5L6AfBAV/K+PcTk07hOfv57+pbXhPTL7LA13xJB4z0/5e7L/eIiAywsiEoHsbfvdOv3dUCzi4Cg==" length="45982017" type="application/octet-stream" />
    </item>
  </channel>
</rss>
