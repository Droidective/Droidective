<?xml version="1.0" encoding="utf-8"?>
<rss version="2.0" xmlns:sparkle="http://www.andymatuschak.org/xml-namespaces/sparkle" xmlns:dc="http://purl.org/dc/elements/1.1/">
  <channel>
    <title>Droidective</title>
    <link>https://droidective.com/appcast.xml</link>
    <description>Most recent Droidective updates.</description>
    <language>en</language>
    <item>
      <title>Droidective 3.8.2</title>
      <sparkle:version>783</sparkle:version>
      <sparkle:shortVersionString>3.8.2</sparkle:shortVersionString>
      <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
      <description><![CDATA[
<style>
:root { color-scheme: light dark; }
body { font: 13px/1.55 -apple-system, BlinkMacSystemFont, sans-serif; margin: 0; }
h2 { font-size: 1.3em; margin: .2em 0 .4em; }
h3 { font-size: 1.05em; margin: 1.1em 0 .3em; }
p { margin: .5em 0; }
ul { margin: .4em 0; padding-left: 1.4em; }
li { margin: .3em 0; }
code { font-family: ui-monospace, SFMono-Regular, Menlo, monospace; font-size: .9em; background: color-mix(in srgb, currentColor 12%, transparent); padding: .1em .35em; border-radius: 4px; }
pre { background: color-mix(in srgb, currentColor 8%, transparent); padding: .6em .8em; border-radius: 6px; overflow-x: auto; }
pre code { background: none; padding: 0; }
a { color: #2f9e44; }
</style>
<p>Fixes for the JS Console in a split pane. The feed left blank space that grew
every time the pane changed width, and the console refused to shrink to the
pane it was given — which read as the tab beside it covering the log.</p>
<h3>JS Console</h3>
<ul>
<li>
<strong>The feed fills its pane again.</strong> A row's height was measured against a
baseline that was still moving, so a message wrapped onto several lines drew
past the height its row had reported. One row is invisible; a feed of them
leaves gaps that shift on every resize. It only showed in a narrow pane,
because that is where rows wrap at all.</li>
<li>
<strong>The console shrinks to the pane it is given.</strong> The connection bar held its
target label at full width — <code>com.myapp.features · Pixel 7 - 17 - API 37</code>
is most of a split pane — and that became the minimum for everything below
it, so rows were laid out wider than the pane and cut off at its edge. The
target and the status line now truncate. This also brings back the filter
row's level, find, export and clear controls, and each row's timestamp, at
the narrowest split.</li>
<li>
<strong>A <code>console.table</code> no longer sizes the whole feed.</strong> Its grid scrolls
inside its own row instead of making every row as wide as itself.</li>
<li>
<strong>Where a log came from is an icon</strong>, in the row's hover controls beside the
two copy buttons. Written out it cost more width than the message beside it
could spare. The file, line, function and full path are in its tooltip, and
clicking it still opens the whole stack.</li>
</ul>
]]></description>
      <pubDate>Fri, 07 Aug 2026 14:12:34 +0000</pubDate>
      <enclosure url="https://github.com/Droidective/Droidective/releases/download/v3.8.2/Droidective-v3.8.2.dmg" sparkle:edSignature="FbSKxPfZWuLRwHs9CyEcXbH+R3Li3/tibv+zo9OGrqpT8KdlD7KKEXrLCheGuJXJIMRq9YTX5KtmUaA3gCerAw==" length="144425801" type="application/octet-stream" />
    </item>
    
  </channel>
</rss>
