<?xml version="1.0" encoding="utf-8"?>
<rss version="2.0" xmlns:sparkle="http://www.andymatuschak.org/xml-namespaces/sparkle" xmlns:dc="http://purl.org/dc/elements/1.1/">
  <channel>
    <title>Droidective</title>
    <link>https://droidective.com/appcast.xml</link>
    <description>Most recent Droidective updates.</description>
    <language>en</language>
    <item>
      <title>Droidective 3.10.0-beta.2</title>
      <sparkle:channel>beta</sparkle:channel>
      <sparkle:version>875</sparkle:version>
      <sparkle:shortVersionString>3.10.0-beta.2</sparkle:shortVersionString>
      <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
      <description><![CDATA[
<style>
:root { color-scheme: light dark; }
body { font: 13px/1.55 -apple-system, BlinkMacSystemFont, sans-serif; margin: 0; }
h2 { font-size: 1.3em; margin: .2em 0 .4em; }
h3 { font-size: 1.05em; margin: 1.1em 0 .3em; }
p { margin: .5em 0; }
ul { margin: .4em 0; padding-left: 1.4em; }
li { margin: .3em 0; }
code { font-family: ui-monospace, SFMono-Regular, Menlo, monospace; font-size: .9em; background: color-mix(in srgb, currentColor 12%, transparent); padding: .1em .35em; border-radius: 4px; }
pre { background: color-mix(in srgb, currentColor 8%, transparent); padding: .6em .8em; border-radius: 6px; overflow-x: auto; }
pre code { background: none; padding: 0; }
a { color: #2f9e44; }
</style>
<p>The first beta shipped a Windows and Linux app that nobody had ever launched.
This one launches it — in CI, on every pull request — and fixes the three
things that stopped it working when somebody finally did.</p>
<h3>Linux, actually running</h3>
<ul>
<li>
<strong>The app now starts, and CI proves it on every change.</strong> A new job installs
the <code>.deb</code> in a bare <code>ubuntu:24.04</code> — so apt resolves the package's own
declared dependencies and nothing the build machine happened to leave behind
— launches it under Xvfb, drives the command palette to open a screen, and
photographs both frames. Every check fails the build rather than warning.</li>
<li>
<strong>The daemon could not start at all.</strong> <code>droidectived</code> was dynamically linked
against the Swift runtime, so on any machine without a Swift toolchain it
died at exec with <code>libswiftCore.so: cannot open shared object file</code>, and the
app came up showing "droidectived would not start" with all thirty-three
screens behind it. It now carries its own runtime.</li>
<li>
<strong>Then it could not find <code>libcurl</code>.</strong> Declared as a dependency of the <code>.deb</code>
rather than assumed.</li>
<li>
<strong>Then it came up and did nothing.</strong> <code>adb devices</code> on a machine whose adb
<em>server</em> is not yet running forks that server and exits — and Linux left the
exited process unreaped, so the call never returned and the window sat at
"0 features" with no error. The process runner now notices that exit itself.
The cold call went from hanging indefinitely to 0.185 s.</li>
</ul>
<h3>Windows</h3>
<ul>
<li>
<strong>It gets launched too, on every beta tag.</strong> The installer runs silently, the
app starts, and the release fails if the app or its daemon is not alive
thirty seconds later. There is no Windows container, so this proves less than
the Linux check does — the runner already has what the toolchain left — but
it is the first time the Windows build has been started at all.</li>
</ul>
<h3>New in the app</h3>
<ul>
<li>
<strong>Three more screens: React Native, Simulate and Connection.</strong> The hub
screens, each matching the Mac's section for section — the RN quick actions
and both Metro paths, Simulate's battery, appearance, layout, locale, network
and proxy, and Connection's live Wi-Fi and IP with the pairing and Private DNS
sections it shares with their standalone screens. With these the sidebar
matches the Mac's.</li>
<li>
<strong>Logcat can follow one app.</strong> Pick it in Apps or press "App on screen", and
the log narrows to that process — filtered on the device with
<code>adb logcat --pid</code>, so the buffer holds only that app's lines instead of
hiding the rest after the fact. It waits for an app that has not launched
yet, and follows it when it relaunches under a new process id.</li>
<li>
<strong>Install App is in the sidebar.</strong> It had a working screen and was missing
from the list, along with four others the daemon was reporting as not built.</li>
</ul>
<h3>Fixed</h3>
<ul>
<li>
<strong>The mirror's codec check is now a real decode.</strong> Linux needs
<code>gstreamer1.0-libav</code> for H.264, which the <code>.deb</code> pulls in by default; without
it the mirror says which package to install rather than showing a black
rectangle. That is now verified by decoding an actual frame rather than
asking the webview whether it thinks it could.</li>
</ul>
<h3>macOS</h3>
<ul>
<li>
<strong>This is a rebuild, not a Mac release.</strong> The Mac app is identical to v3.9.2
and is offered only to installs that opted into beta updates in Settings ▸
General. The stable channel is untouched: the download button and
<code>/releases/latest/download/</code> keep serving v3.9.2.</li>
</ul>
]]></description>
      <pubDate>Sun, 30 Aug 2026 14:48:34 +0000</pubDate>
      <enclosure url="https://github.com/Droidective/Droidective/releases/download/v3.10.0-beta.2/Droidective-v3.10.0-beta.2.dmg" sparkle:edSignature="gG0Ouw12UrR/j91lNhssZeoA2fIN/4ns/Xv7/MvnAL0HjJi95vPyYz/qlL9uvYghXlxKLa9Bcjs/1EZpe1QRBw==" length="145117761" type="application/octet-stream" />
    </item>
    <item>
      <title>Droidective 3.9.2</title>
      <sparkle:version>830</sparkle:version>
      <sparkle:shortVersionString>3.9.2</sparkle:shortVersionString>
      <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
      <description><![CDATA[
<style>
:root { color-scheme: light dark; }
body { font: 13px/1.55 -apple-system, BlinkMacSystemFont, sans-serif; margin: 0; }
h2 { font-size: 1.3em; margin: .2em 0 .4em; }
h3 { font-size: 1.05em; margin: 1.1em 0 .3em; }
p { margin: .5em 0; }
ul { margin: .4em 0; padding-left: 1.4em; }
li { margin: .3em 0; }
code { font-family: ui-monospace, SFMono-Regular, Menlo, monospace; font-size: .9em; background: color-mix(in srgb, currentColor 12%, transparent); padding: .1em .35em; border-radius: 4px; }
pre { background: color-mix(in srgb, currentColor 8%, transparent); padding: .6em .8em; border-radius: 6px; overflow-x: auto; }
pre code { background: none; padding: 0; }
a { color: #2f9e44; }
</style>
<p>Pulling events out of a busy log. Rows in the Reactotron timeline and the JS
Console can be selected — ⌘-click one, ⇧-click a range, drag across them — and
copied whole, payload included. An API call's status code also reads off its
collapsed row, and the empty "Screen Mirror" window that could appear at launch
is gone.</p>
<h3>Reactotron and JS Console</h3>
<ul>
<li>
<strong>Select rows and copy them whole.</strong> Pulling a few events out of a busy
timeline meant right-clicking them one at a time. ⌘-click toggles a row,
⇧-click takes the range from the last one picked, and dragging across rows
sweeps a range; the toolbar shows how many are picked, with a copy menu, and
⌘C copies them. A copy carries the whole event rather than the line already on
screen — the row's summary followed by the complete payload: an API call's
request body, headers and response, an action's or saga's arguments. Copy as
JSON gives the same events in the export's wire form.</li>
<li>
<strong>A selection can't copy what the feed no longer has.</strong> Rows that were
cleared, filtered out, or trimmed while the selection stood are dropped from
it, so a copy never contains events nobody can see.</li>
<li>
<strong>The gestures stay out of each other's way.</strong> Only a row's header carries the
drag, so dragging through an expanded payload still selects that text, and ⌘C
is bound only while rows are picked — it never shadows copying text out of the
search field or an open payload.</li>
</ul>
<h3>Reactotron</h3>
<ul>
<li>
<strong>An API event's status code sits on its collapsed row.</strong> Reading a timeline is
mostly looking for the one call that didn't return 200, and that meant
expanding rows one at a time. The code now sits between the API badge and the
method — green for 2xx, orange for a client error, red for a server error,
neutral for a redirect. Only the code is tinted, so the timeline's own badge
colours still read. A request that never got a response reports status 0 on the
wire, so it reads <code>ERR</code>.</li>
</ul>
<h3>Mirror</h3>
<ul>
<li>
<strong>No empty "Screen Mirror" window at launch.</strong> A 420×850 window with a title
bar and nothing in it could appear on its own at launch — and in the worst case
it was the only window the app had. The pop-out mirror's window group persists
the device it was presented with and re-presents it at the next launch, before
any workspace exists for it to attach to, so it rendered nothing and no main
window was created either. A pop-out now has to have been asked for in this
session; anything else closes itself once a real window is up.</li>
</ul>
]]></description>
      <pubDate>Tue, 25 Aug 2026 12:15:04 +0000</pubDate>
      <enclosure url="https://github.com/Droidective/Droidective/releases/download/v3.9.2/Droidective-v3.9.2.dmg" sparkle:edSignature="9J2wA5//XpPHq08jgJerK9+IaIjjFOWN3+WYCH2sjO9DDOtLfcAbjhNFbDU/K4VORPico+VsbjbVUI5Kj4uxAw==" length="145155632" type="application/octet-stream"/>
    </item>
  </channel>
</rss>
