<?xml version="1.0" encoding="utf-8"?>
<rss version="2.0" xmlns:sparkle="http://www.andymatuschak.org/xml-namespaces/sparkle" xmlns:dc="http://purl.org/dc/elements/1.1/">
  <channel>
    <title>Droidective</title>
    <link>https://droidective.com/appcast.xml</link>
    <description>Most recent Droidective updates.</description>
    <language>en</language>
    <item>
      <title>Droidective 3.0.0</title>
      <sparkle:version>369</sparkle:version>
      <sparkle:shortVersionString>3.0.0</sparkle:shortVersionString>
      <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
      <description><![CDATA[
<style>
:root { color-scheme: light dark; }
body { font: 13px/1.55 -apple-system, BlinkMacSystemFont, sans-serif; margin: 0; }
h2 { font-size: 1.3em; margin: .2em 0 .4em; }
h3 { font-size: 1.05em; margin: 1.1em 0 .3em; }
p { margin: .5em 0; }
ul { margin: .4em 0; padding-left: 1.4em; }
li { margin: .3em 0; }
code { font-family: ui-monospace, SFMono-Regular, Menlo, monospace; font-size: .9em; background: color-mix(in srgb, currentColor 12%, transparent); padding: .1em .35em; border-radius: 4px; }
pre { background: color-mix(in srgb, currentColor 8%, transparent); padding: .6em .8em; border-radius: 6px; overflow-x: auto; }
pre code { background: none; padding: 0; }
a { color: #2f9e44; }
</style>
<p>Terminal split panes with working-directory inheritance, an onboarding tour
built from recordings of the real app, redesigned React Native and Connection
hubs, custom commands through your login shell, and JS Console reload/restart.</p>
<h3>New features</h3>
<ul>
<li>
<strong>Terminal split panes</strong> — ⌘D splits the focused shell side by side, ⇧⌘D
stacks it (File menu, tab context menus, or the terminal's right-click menu).
Splits nest, ⌘W closes the pane first and then the tab, <code>exit</code> folds a pane
back into its siblings, and the surviving shell takes keyboard focus.</li>
<li>
<strong>New shells open where you are</strong> — new tabs and split panes start in the
focused shell's live working directory, read from the kernel, so no OSC 7 or
shell configuration is needed.</li>
<li>
<strong>Terminal tabs on top</strong> — a toggle switches the tab list between the left
rail and a Chrome-style strip along the top, keeping groups, context menus,
and drag-reorder in both layouts.</li>
<li>
<strong>Custom commands run in your login shell</strong> — shell commands source your
interactive rc, so <code>.zshrc</code>/<code>.bashrc</code> aliases and PATH additions resolve.
Each command also chooses where it runs: silently with a toast, or typed
into a fresh in-app Terminal tab with live output.</li>
<li>
<strong>JS Console: Reload JS and Restart app</strong> — reload the running bundle over
CDP (with a dev-menu fallback) or force-stop and relaunch the target app,
with a searchable app picker when detection can't find it.</li>
<li>
<strong>React Native hub redesign</strong> — quick actions are cards with visible
descriptions, and Metro forwarding gets its own section with a port field
(prefilled 8081) beside USB / Wi-Fi dev-server rows. The device bar shows
the bundle pill on the hub, so Deep Links and Process Death target a
visible, changeable bundle.</li>
<li>
<strong>Connection hub shows your device's network</strong> — "This device" replaces the
blind copy button with the live Wi-Fi network name and IP, a Copy IP
button, and a refresh accessory.</li>
<li>
<strong>Pinned section on Home</strong> — the launchpad follows the sidebar's order and
mirrors its pinned features; Home itself becomes a permanent house icon
leading the tab strip instead of a closable tab.</li>
</ul>
<h3>Improvements</h3>
<ul>
<li>
<strong>The onboarding tour is rebuilt</strong> around five pages, each led by a looping
screen recording of the real app — sidebar, tabs and drag-to-split, roles,
settings and hotkeys, and the Quick Actions panel summoned over a browser.
Skip from any page jumps to the hotkey ask, and Reduce Motion holds a still
frame. Reactotron gets a one-time intro demo on first open.</li>
<li>
<strong>Reactotron timeline</strong> — filters move into a Timeline Filter dialog
matching the Reactotron app's (grouped checkboxes with method/status
refinements), rows preview object payloads inline with a hover copy button,
the log text is selectable, and Restart app auto-detects the running app.
In single-pane mode the timeline sits under two bars instead of three.</li>
<li>
<strong>Steady with huge console payloads</strong> — a multi-megabyte <code>console.log</code> no
longer stalls the JS Console: rows render bounded previews and the buffer
gets a byte budget on top of its entry cap.</li>
<li>
<strong>Idle toolbars stay out of the way</strong> — Logcat's filter toolbar hides until
a device connects, and the JS Console's until it's connected or holds
output.</li>
<li>
<strong>Device-bar pills size to their content</strong> — short labels hug, long device
names and package ids truncate, and the bar compresses cleanly in narrow
windows.</li>
<li>The main window keeps at least 85% of the screen's visible frame, and the
role picker can be closed with Esc when reopened from Home instead of
resetting your tabs.</li>
</ul>
<h3>Fixes</h3>
<ul>
<li>
<strong>Nothing clips at the minimum window width</strong> — on tabs showing the bundle
pill, the device bar could push the layout past the window, cutting off
sidebar icons on the left and bar controls on the right.</li>
<li>
<strong>Dropping a tab no longer leaves a stale highlight</strong> — the ending drag
session could re-light a pane's drop highlight or a reorder guideline right
after the drop. Tab drags also read better now: the dragged chip fades in
place while its ghost rides the cursor, and reordered chips slide instead
of snapping.</li>
</ul>
<p>Installed copies update in place via Sparkle.</p>
]]></description>
      <pubDate>Sun, 12 Jul 2026 01:42:06 +0000</pubDate>
      <enclosure url="https://github.com/Droidective/Droidective/releases/download/v3.0.0/Droidective-v3.0.0.dmg" sparkle:edSignature="De8kZaAjzOVOZzCFELokB7FNJCmh0qNC8zrtOJkNCA/JUyW4SzNdNYQL7fAPA7H8ohZG9bjb4la73A9BIQ0PDg==" length="51282149" type="application/octet-stream" />
    </item>
  </channel>
</rss>
