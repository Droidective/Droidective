<?xml version="1.0" encoding="utf-8"?>
<rss version="2.0" xmlns:sparkle="http://www.andymatuschak.org/xml-namespaces/sparkle" xmlns:dc="http://purl.org/dc/elements/1.1/">
  <channel>
    <title>Droidective</title>
    <link>https://droidective.com/appcast.xml</link>
    <description>Most recent Droidective updates.</description>
    <language>en</language>
    <item>
      <title>Droidective 3.10.0-beta.4</title>
      <sparkle:channel>beta</sparkle:channel>
      <sparkle:version>901</sparkle:version>
      <sparkle:shortVersionString>3.10.0-beta.4</sparkle:shortVersionString>
      <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
      <description><![CDATA[
<style>
:root { color-scheme: light dark; }
body { font: 13px/1.55 -apple-system, BlinkMacSystemFont, sans-serif; margin: 0; }
h2 { font-size: 1.3em; margin: .2em 0 .4em; }
h3 { font-size: 1.05em; margin: 1.1em 0 .3em; }
p { margin: .5em 0; }
ul { margin: .4em 0; padding-left: 1.4em; }
li { margin: .3em 0; }
code { font-family: ui-monospace, SFMono-Regular, Menlo, monospace; font-size: .9em; background: color-mix(in srgb, currentColor 12%, transparent); padding: .1em .35em; border-radius: 4px; }
pre { background: color-mix(in srgb, currentColor 8%, transparent); padding: .6em .8em; border-radius: 6px; overflow-x: auto; }
pre code { background: none; padding: 0; }
a { color: #2f9e44; }
</style>
<p>Five more of the Mac's features reach Windows and Linux, and two of them could
not simply be moved across — the recorder and the file drops both needed a
different answer here than the one macOS gave. Everything else is the same
screen, the same wording, the same gestures.</p>
<h3>API Testing</h3>
<ul>
<li>
<strong>The whole client is here.</strong> Collections and nested folders, environments,
<code>{{variable}}</code> scopes with the unresolved ones surfaced before a send rather
than sent as literal braces, assertions on status, timing, body, header and
JSON path, a collection runner, code generation to six targets, Postman
import and export, and a cURL paste that parses back into a request.</li>
<li>
<strong>The collection runner deliberately stayed in the app.</strong> It would have been
one daemon call; instead the client walks the tree and sends one request at a
time, so progress is live and Stop is instant rather than "the run finishes
and then tells you".</li>
</ul>
<h3>Recording the screen</h3>
<ul>
<li>
<strong>The recorder is a new pipeline, not a port.</strong> The Mac records through its
Apple-only media stack, so there was nothing to move: the daemon now feeds
the scrcpy H.264 stream straight into ffmpeg and copies the video track
without re-encoding. Pause and resume produce segments that the concat
demuxer joins at the end, so a paused take is one file.</li>
<li>
<strong>A recording said 30 seconds for a 12-second file.</strong> The clock had been
running from the button press, and scrcpy takes a few seconds to produce its
first frame. It is measured from the first frame to the last now — 13.820
reported against 13.819 in the file.</li>
<li>
<strong>ffmpeg is downloaded rather than bundled.</strong> Digest-verified from a pinned
release, <code>.tar.xz</code> on Linux and <code>.zip</code> on Windows, which is also what
unblocked <strong>Settings ▸ Tools</strong> — the managed-tool store, with sizes, upgrades
and removal.</li>
</ul>
<h3>More than one window</h3>
<ul>
<li>
<strong>One window per device</strong>, each with its own tabs, its own arrangement and
its own device selection, restored where you left them.</li>
<li>
<strong>The Focus / Take Over banner</strong> for the features that cannot run twice on
one device — the mirror, the recorder, the JS console — instead of two
windows racing each other for the same encoder.</li>
<li>
<strong>Every window after the first tints its device icon</strong>, so which window you
are looking at is answerable without reading the tab strip.</li>
</ul>
<h3>Files dropped on the window</h3>
<ul>
<li>
<strong>An APK or app bundle dropped anywhere installs it</strong>, whatever screen is in
front; anything else dropped on the File Explorer is pushed to the directory
it is showing. Anywhere else says which of the two to do rather than guessing
a destination on someone else's device.</li>
<li>
<strong>The in-app drags were the constraint.</strong> Turning on the toolkit's own
file-drop handler stops every drag inside the page working — the tab strip,
the sidebar, the mirror wall — so instead of trading one for the other, a
dropped file's <em>bytes</em> are staged on the way through. One copy, and nothing
that already worked had to be rewritten.</li>
</ul>
<h3>Look and first launch</h3>
<ul>
<li>
<strong>The window can be glass.</strong> Settings ▸ Appearance ▸ Window gains Opacity and
Grain — the Mac's sliders, with the Mac's arithmetic, so a window set up on
one machine looks the same on the other. At 100% nothing changes at all.</li>
<li>
<strong>Blur is a switch here, not a slider.</strong> No desktop platform lets an
application ask for a blur <em>radius</em>: Windows has Acrylic, which is on or off,
and on Linux the compositor decides entirely. A slider with two positions
would be a worse lie than a switch, so the row says what the platform will
actually do.</li>
<li>
<strong>First launch asks what you do.</strong> The role picker curates the sidebar to
Android, React Native, iOS, QA, support or security work — with the tool
counts following the "I work with React Native" switch live — and Settings ▸
General names the role in effect and lets you change it. Escape leaves it.</li>
</ul>
<h3>Fixes</h3>
<ul>
<li>
<strong>A form action with two devices connected was a Run button that did
nothing.</strong> Only one screen can be in front of the Quick Actions panel, and it
checked the form first, so the pick-a-device step opened behind the form that
had just been submitted. Found while adding the step that asks <em>which app</em> an
action that needs one should run against.</li>
<li>
<strong>The Linux smoke test was photographing the wrong thing.</strong> It drives the app
on every pull request, and once the role picker appeared on first launch its
keystrokes were going into that instead. It now dismisses the picker and
proves it went away before driving anything, so the job checks three things
where it had been checking one.</li>
</ul>
]]></description>
      <pubDate>Mon, 31 Aug 2026 20:05:02 +0000</pubDate>
      <enclosure url="https://github.com/Droidective/Droidective/releases/download/v3.10.0-beta.4/Droidective-v3.10.0-beta.4.dmg" sparkle:edSignature="qej8n9oTVg5NA5RiAMW9S/fRGOHT8HNjqSoI/yOs0tinf5EFX7Hf/MFt8Eju2qJzLMdJnTssT4FQ4q6Ghfr1Cw==" length="145188178" type="application/octet-stream" />
    </item>
    <item>
      <title>Droidective 3.9.2</title>
      <sparkle:version>830</sparkle:version>
      <sparkle:shortVersionString>3.9.2</sparkle:shortVersionString>
      <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
      <description><![CDATA[
<style>
:root { color-scheme: light dark; }
body { font: 13px/1.55 -apple-system, BlinkMacSystemFont, sans-serif; margin: 0; }
h2 { font-size: 1.3em; margin: .2em 0 .4em; }
h3 { font-size: 1.05em; margin: 1.1em 0 .3em; }
p { margin: .5em 0; }
ul { margin: .4em 0; padding-left: 1.4em; }
li { margin: .3em 0; }
code { font-family: ui-monospace, SFMono-Regular, Menlo, monospace; font-size: .9em; background: color-mix(in srgb, currentColor 12%, transparent); padding: .1em .35em; border-radius: 4px; }
pre { background: color-mix(in srgb, currentColor 8%, transparent); padding: .6em .8em; border-radius: 6px; overflow-x: auto; }
pre code { background: none; padding: 0; }
a { color: #2f9e44; }
</style>
<p>Pulling events out of a busy log. Rows in the Reactotron timeline and the JS
Console can be selected — ⌘-click one, ⇧-click a range, drag across them — and
copied whole, payload included. An API call's status code also reads off its
collapsed row, and the empty "Screen Mirror" window that could appear at launch
is gone.</p>
<h3>Reactotron and JS Console</h3>
<ul>
<li>
<strong>Select rows and copy them whole.</strong> Pulling a few events out of a busy
timeline meant right-clicking them one at a time. ⌘-click toggles a row,
⇧-click takes the range from the last one picked, and dragging across rows
sweeps a range; the toolbar shows how many are picked, with a copy menu, and
⌘C copies them. A copy carries the whole event rather than the line already on
screen — the row's summary followed by the complete payload: an API call's
request body, headers and response, an action's or saga's arguments. Copy as
JSON gives the same events in the export's wire form.</li>
<li>
<strong>A selection can't copy what the feed no longer has.</strong> Rows that were
cleared, filtered out, or trimmed while the selection stood are dropped from
it, so a copy never contains events nobody can see.</li>
<li>
<strong>The gestures stay out of each other's way.</strong> Only a row's header carries the
drag, so dragging through an expanded payload still selects that text, and ⌘C
is bound only while rows are picked — it never shadows copying text out of the
search field or an open payload.</li>
</ul>
<h3>Reactotron</h3>
<ul>
<li>
<strong>An API event's status code sits on its collapsed row.</strong> Reading a timeline is
mostly looking for the one call that didn't return 200, and that meant
expanding rows one at a time. The code now sits between the API badge and the
method — green for 2xx, orange for a client error, red for a server error,
neutral for a redirect. Only the code is tinted, so the timeline's own badge
colours still read. A request that never got a response reports status 0 on the
wire, so it reads <code>ERR</code>.</li>
</ul>
<h3>Mirror</h3>
<ul>
<li>
<strong>No empty "Screen Mirror" window at launch.</strong> A 420×850 window with a title
bar and nothing in it could appear on its own at launch — and in the worst case
it was the only window the app had. The pop-out mirror's window group persists
the device it was presented with and re-presents it at the next launch, before
any workspace exists for it to attach to, so it rendered nothing and no main
window was created either. A pop-out now has to have been asked for in this
session; anything else closes itself once a real window is up.</li>
</ul>
]]></description>
      <pubDate>Tue, 25 Aug 2026 12:15:04 +0000</pubDate>
      <enclosure url="https://github.com/Droidective/Droidective/releases/download/v3.9.2/Droidective-v3.9.2.dmg" sparkle:edSignature="9J2wA5//XpPHq08jgJerK9+IaIjjFOWN3+WYCH2sjO9DDOtLfcAbjhNFbDU/K4VORPico+VsbjbVUI5Kj4uxAw==" length="145155632" type="application/octet-stream"/>
    </item>
  </channel>
</rss>
