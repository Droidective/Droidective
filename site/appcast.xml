<?xml version="1.0" encoding="utf-8"?>
<rss version="2.0" xmlns:sparkle="http://www.andymatuschak.org/xml-namespaces/sparkle" xmlns:dc="http://purl.org/dc/elements/1.1/">
  <channel>
    <title>Droidective</title>
    <link>https://droidective.com/appcast.xml</link>
    <description>Most recent Droidective updates.</description>
    <language>en</language>
    <item>
      <title>Droidective 3.9.0</title>
      <sparkle:version>795</sparkle:version>
      <sparkle:shortVersionString>3.9.0</sparkle:shortVersionString>
      <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
      <description><![CDATA[
<style>
:root { color-scheme: light dark; }
body { font: 13px/1.55 -apple-system, BlinkMacSystemFont, sans-serif; margin: 0; }
h2 { font-size: 1.3em; margin: .2em 0 .4em; }
h3 { font-size: 1.05em; margin: 1.1em 0 .3em; }
p { margin: .5em 0; }
ul { margin: .4em 0; padding-left: 1.4em; }
li { margin: .3em 0; }
code { font-family: ui-monospace, SFMono-Regular, Menlo, monospace; font-size: .9em; background: color-mix(in srgb, currentColor 12%, transparent); padding: .1em .35em; border-radius: 4px; }
pre { background: color-mix(in srgb, currentColor 8%, transparent); padding: .6em .8em; border-radius: 6px; overflow-x: auto; }
pre code { background: none; padding: 0; }
a { color: #2f9e44; }
</style>
<p>Mirroring several devices at once. The Mirror Wall shows up to six connected
devices side by side in one pane, each one live and controllable, and Full View
hands the whole display to whatever screen you are on. Mirror sessions also stop
leaking their adb tunnel when the app quits — which affected the single mirror
too.</p>
<h3>Mirror Wall</h3>
<ul>
<li>
<strong>Up to six devices, side by side.</strong> The 61st tool mirrors several connected
devices in one pane. Every tile is the same session the full mirror runs, so
clicking a tile taps that device and typing goes to it — one wall for a whole
bench of phones instead of one window per device.</li>
<li>
<strong>The wall picks its own devices.</strong> A Devices menu lists the connected
Android devices with a checkbox each, capped at six; the picked set and the
order they sit in are remembered per window. Opening the wall for the first
time shows what is connected.</li>
<li>
<strong>Automatic or fixed layout.</strong> Auto derives the column count from the pane
width and drops a column rather than shrinking tiles into uselessness; 1, 2
or 3 columns is a fixed choice that stands. Tiles rearrange by dragging a
tile's caption strip — the strip, not the video, so a swipe on the device
stays a swipe.</li>
<li>
<strong>Any tile can become its own window.</strong> Open one device in its own window, or
every tile at once, and Arrange Mirror Windows tiles them across the screen.
Bring Back closes the window and the tile picks the device up again. A pop-out
mirror window is now pinned to the device it was opened for rather than
following the device bar, which is what lets several stand side by side.</li>
<li>
<strong>Six encoders is a real cost, so the wall spends less per tile.</strong> The
resolution and frame rate each tile asks the device for step down as tiles are
added (one tile is exactly the full mirror), tiles pause individually, audio
streams from the focused tile only and is off by default, and the whole wall
stops after two minutes behind another tab — or behind another window, or
minimised.</li>
<li>
<strong>One device, one mirror.</strong> A device already showing in its own window, in the
Mirror Screen tab, or in another workspace window says so on its tile with a
way to reach it, instead of putting a second encoder on the device.</li>
</ul>
<h3>Full View</h3>
<ul>
<li>
<strong>View ▸ Full View (⇧⌘F) gives a screen the whole display.</strong> The sidebar,
device bar and tab strip go away and the window enters full screen — six
mirror tiles want every pixel. The feature's own controls stay, so the wall
keeps the row with its Devices menu, layout picker and the way back out.</li>
</ul>
<h3>Fixes</h3>
<ul>
<li>
<strong>Mirror sessions released their adb tunnel on quit.</strong> Each live mirror opens
an <code>adb forward</code> tunnel that its teardown removes, but at quit the app exited
before the teardown ran, leaving the tunnel registered in the adb server —
one per session, every quit, until <code>adb kill-server</code>. Quit now waits for the
sessions to close. This affected the single mirror and the pop-out window, not
just the wall.</li>
<li>
<strong>Reconnecting a mirror tile showed a black screen.</strong> The tile kept drawing
the stopped session's video layer, so input worked while no frame ever
arrived.</li>
</ul>
]]></description>
      <pubDate>Tue, 18 Aug 2026 09:02:55 +0000</pubDate>
      <enclosure url="https://github.com/Droidective/Droidective/releases/download/v3.9.0/Droidective-v3.9.0.dmg" sparkle:edSignature="onEUjSNOZEy5ii/jbLm8BlOF0eGpGgUwNZzcmt+FzNbyei5UQewMDctdqnacuMmCXIjCQdmAZ2vwdXTWvyL6CQ==" length="144872996" type="application/octet-stream" />
    </item>
    
  </channel>
</rss>
