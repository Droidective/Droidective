<?xml version="1.0" encoding="utf-8"?>
<rss version="2.0" xmlns:sparkle="http://www.andymatuschak.org/xml-namespaces/sparkle" xmlns:dc="http://purl.org/dc/elements/1.1/">
  <channel>
    <title>Droidective</title>
    <link>https://droidective.com/appcast.xml</link>
    <description>Most recent Droidective updates.</description>
    <language>en</language>
    <item>
      <title>Droidective 3.3.0</title>
      <sparkle:version>444</sparkle:version>
      <sparkle:shortVersionString>3.3.0</sparkle:shortVersionString>
      <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
      <description><![CDATA[
<style>
:root { color-scheme: light dark; }
body { font: 13px/1.55 -apple-system, BlinkMacSystemFont, sans-serif; margin: 0; }
h2 { font-size: 1.3em; margin: .2em 0 .4em; }
h3 { font-size: 1.05em; margin: 1.1em 0 .3em; }
p { margin: .5em 0; }
ul { margin: .4em 0; padding-left: 1.4em; }
li { margin: .3em 0; }
code { font-family: ui-monospace, SFMono-Regular, Menlo, monospace; font-size: .9em; background: color-mix(in srgb, currentColor 12%, transparent); padding: .1em .35em; border-radius: 4px; }
pre { background: color-mix(in srgb, currentColor 8%, transparent); padding: .6em .8em; border-radius: 6px; overflow-x: auto; }
pre code { background: none; padding: 0; }
a { color: #2f9e44; }
</style>
<p>A JS console that stays connected, a screen mirror that no longer eats CPU
after you leave it, logcat search split into separate Filter and Find, and a
first iOS feature: streaming a simulator's native logs.</p>
<h3>Logcat: Filter vs Find</h3>
<ul>
<li>
<strong>Filter and Find are now separate</strong> — the toolbar field <em>filters</em> (shows
only matching lines, as before); a new find bar (⌘F) <em>finds</em>: it highlights
matches and steps through them with ⌘G/⇧⌘G and a "2 of 7" counter without
hiding anything.</li>
<li>
<strong>One app selector</strong> — the App menu inside logcat now does everything the
bar's bundle pill did (pick a saved bundle, add from the device's installed
apps, grab the app on screen, manage), and the bundle pill no longer
doubles up above it.</li>
</ul>
<h3>iOS Logs (new)</h3>
<ul>
<li>
<strong>Stream a simulator's unified log</strong> — the new iOS Logs feature tails a
booted iOS Simulator's native logs (<code>log stream</code>) in the same pane logcat
uses: level picker, text filter, the ⌘F find bar, export, and right-click
to filter by process.</li>
</ul>
<h3>JS Console stays connected</h3>
<ul>
<li>
<strong>No more idle disconnects</strong> — a silent keepalive satisfies React Native's
inspector-proxy heartbeat, which a plain WebSocket ping doesn't.</li>
<li>
<strong>Reconnects survive app relaunches, phone sleep, and Metro restarts</strong> —
auto-reconnect no longer strands itself on a stale device id.</li>
<li>
<strong>Plays nice with React Native DevTools</strong> — when another debugger takes
over (RN ≤ 0.84 allows one), the console stands down with a notice instead
of fighting for the session; on RN 0.85+ both attach side by side.</li>
<li>
<strong>No duplicate logs on reconnect</strong> — Hermes' replayed console history is
deduplicated, so the feed no longer doubles after each reconnect.</li>
</ul>
<h3>Screen mirror</h3>
<ul>
<li>
<strong>Fixed: runaway CPU after using the mirror</strong> — switching away from the
mirror while it was still connecting could leak a session that kept
streaming and decoding until the app quit (the reported 500%+ CPU). Leaked
sessions and stray adb tunnels are gone.</li>
<li>
<strong>Lower latency</strong> — frames render as they decode, touch input isn't
batched by the TCP stack, and the slow-motion catch-up after opening the
mirror is gone.</li>
<li>
<strong>Device audio is now opt-in</strong> — the mirror starts video-only; the Audio
switch streams device audio too (Android 11+). The choice persists.</li>
</ul>
<h3>Improvements</h3>
<ul>
<li>
<strong>Device dropdown refreshes itself</strong> — opening it re-scans for devices;
the separate refresh button appears only when nothing is connected.</li>
<li>
<strong>Smooth resizing</strong> — dragging the sidebar edge and the split-pane divider
no longer jitters.</li>
<li>
<strong>Newest-first logs</strong> — Logcat, JS Console, and Reactotron can flip to
newest-at-top.</li>
<li>
<strong>Beta update channel</strong> — Settings ▸ General ▸ Updates gains an opt-in
beta channel; opting out never downgrades.</li>
<li>Reactotron's Copy as cURL keeps query params and form fields, timeline
frames stay in wire order, and large frames no longer truncate.</li>
<li>Pulling an app's APK grabs every split APK, not just base.apk.</li>
<li>Device platforms, features, and the Emulators screen scope to your role.</li>
</ul>
]]></description>
      <pubDate>Wed, 15 Jul 2026 11:47:03 +0000</pubDate>
      <enclosure url="https://github.com/Droidective/Droidective/releases/download/v3.3.0/Droidective-v3.3.0.dmg" sparkle:edSignature="P/fqogk1wEYyrotfhjbExOavloN7M9dtUwZzkdEOehT5G0/lSCB83GbEbXx5ycTGGSIDEmjefZq7fzAoQPnXCA==" length="52446696" type="application/octet-stream" />
    </item>
    
  </channel>
</rss>
