<?xml version="1.0" encoding="utf-8"?>
<rss version="2.0" xmlns:sparkle="http://www.andymatuschak.org/xml-namespaces/sparkle" xmlns:dc="http://purl.org/dc/elements/1.1/">
  <channel>
    <title>Droidective</title>
    <link>https://droidective.com/appcast.xml</link>
    <description>Most recent Droidective updates.</description>
    <language>en</language>
    <item>
      <title>Droidective 3.3.0-beta.1</title>
      <sparkle:channel>beta</sparkle:channel>
      <sparkle:version>429</sparkle:version>
      <sparkle:shortVersionString>3.3.0-beta.1</sparkle:shortVersionString>
      <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
      <description><![CDATA[
<style>
:root { color-scheme: light dark; }
body { font: 13px/1.55 -apple-system, BlinkMacSystemFont, sans-serif; margin: 0; }
h2 { font-size: 1.3em; margin: .2em 0 .4em; }
h3 { font-size: 1.05em; margin: 1.1em 0 .3em; }
p { margin: .5em 0; }
ul { margin: .4em 0; padding-left: 1.4em; }
li { margin: .3em 0; }
code { font-family: ui-monospace, SFMono-Regular, Menlo, monospace; font-size: .9em; background: color-mix(in srgb, currentColor 12%, transparent); padding: .1em .35em; border-radius: 4px; }
pre { background: color-mix(in srgb, currentColor 8%, transparent); padding: .6em .8em; border-radius: 6px; overflow-x: auto; }
pre code { background: none; padding: 0; }
a { color: #2f9e44; }
</style>
<p>The first beta-channel release. The headline is a JS console that stays
connected: four root causes of the repeated disconnects are fixed, verified
against React Native 0.82 through 0.86.</p>
<h3>JS Console stays connected</h3>
<ul>
<li>
<strong>No more idle disconnects</strong> — React Native's inspector proxy kills a
debugger socket that stays quiet through its heartbeat window, and a
WebSocket ping doesn't count. The console now sends a tiny silent no-op
evaluate as a keepalive, paced so even a wedged JS thread can't stall it.</li>
<li>
<strong>Reconnects survive app relaunches, phone sleep, and Metro restarts</strong> —
the proxy hands the app a fresh device id whenever it re-registers; the
console now falls back to the app's id, so auto-reconnect no longer strands
itself on a stale id.</li>
<li>
<strong>Plays nice with React Native DevTools</strong> — when another debugger takes the
app over (RN ≤ 0.84 allows only one), the console stands down with a notice
instead of stealing the session back in a loop. On RN 0.85+ both attach
side by side.</li>
<li>
<strong>No duplicate logs on reconnect</strong> — Hermes replays its console history on
every attach; a replay gate drops the already-shown portion, so the feed no
longer doubles after each reconnect.</li>
<li>
<strong>Newest-first, when you want it</strong> — Logcat, JS Console, and Reactotron
gain a reverse-order button.</li>
</ul>
<h3>Beta update channel</h3>
<ul>
<li>
<strong>Receive beta updates</strong> — Settings ▸ General ▸ Updates gains an opt-in
beta channel (how you're reading this). Opting in checks immediately;
opting out never downgrades — the app simply waits for the next stable.</li>
</ul>
<h3>Also in this beta</h3>
<ul>
<li>Device platforms, features, and the Emulators screen scope to your role.</li>
<li>Reactotron connection gaps and truncated timeline rows are fixed.</li>
<li>The Settings update toggles now render their state correctly.</li>
<li>Gallery tiles align to a uniform 16:10 crop.</li>
</ul>
<hr>
]]></description>
      <pubDate>Wed, 15 Jul 2026 06:32:59 +0000</pubDate>
      <enclosure url="https://github.com/Droidective/Droidective/releases/download/v3.3.0-beta.1/Droidective-v3.3.0-beta.1.dmg" sparkle:edSignature="8Uhw8P5Cqa/uYhCJ2Okxt0Qn3PnGK/8ZBWAGCrsAoqsIippfHM6xOrojHuepK7waRNj4HeCx6hlzRbfFveaiBg==" length="52594832" type="application/octet-stream" />
    </item>
    <item>
      <title>Droidective 3.2.0</title>
      <sparkle:version>409</sparkle:version>
      <sparkle:shortVersionString>3.2.0</sparkle:shortVersionString>
      <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
      <description><![CDATA[
<style>
:root { color-scheme: light dark; }
body { font: 13px/1.55 -apple-system, BlinkMacSystemFont, sans-serif; margin: 0; }
h2 { font-size: 1.3em; margin: .2em 0 .4em; }
h3 { font-size: 1.05em; margin: 1.1em 0 .3em; }
p { margin: .5em 0; }
ul { margin: .4em 0; padding-left: 1.4em; }
li { margin: .3em 0; }
code { font-family: ui-monospace, SFMono-Regular, Menlo, monospace; font-size: .9em; background: color-mix(in srgb, currentColor 12%, transparent); padding: .1em .35em; border-radius: 4px; }
pre { background: color-mix(in srgb, currentColor 8%, transparent); padding: .6em .8em; border-radius: 6px; overflow-x: auto; }
pre code { background: none; padding: 0; }
a { color: #2f9e44; }
</style>
<p>Wireless debugging moves into the device dropdown: pair and connect a device
over Wi-Fi from anywhere in the app, with a guided flow for each path.</p>
<h3>Connect over Wi-Fi</h3>
<ul>
<li>
<strong>Pair &amp; connect from the device dropdown</strong> — the device menu gains a
"Wireless debugging" section with <em>Pair new device…</em> and <em>Connect to
device…</em>, opening a guided sheet with three paths:
<ul>
<li>
<strong>Pair new device</strong> — numbered steps that mirror the phone's Android 11+
pairing dialog: where to find it, one field for the <code>ip:port</code> it shows plus
the 6-digit code, then a connect step that explains the connection port
differs from the pairing port (the host carries over after pairing).</li>
<li>
<strong>Already paired</strong> — a single <code>ip:port</code> field for a device that was paired
before or is already in tcpip mode.</li>
<li>
<strong>Via USB cable</strong> — one click switches a plugged-in device to Wi-Fi
debugging and connects; the device list updates live.</li>
</ul>
</li>
<li>
<strong>Paste-friendly addresses</strong> — every address field takes the exact
"IP address &amp; Port" string the phone displays (IPv6 and stray whitespace
included); buttons stay disabled until the input is valid.</li>
<li>
<strong>Clear outcomes</strong> — adb's own reason is shown inline on failure (wrong
code, unreachable host, an adb too old to pair), and a successful
connection selects the new device and confirms in a notification.</li>
</ul>
<h3>Fixes</h3>
<ul>
<li>The tab strip's Home button no longer sits flush against its divider.</li>
</ul>
<hr>
]]></description>
      <pubDate>Tue, 14 Jul 2026 09:23:49 +0000</pubDate>
      <enclosure url="https://github.com/Droidective/Droidective/releases/download/v3.2.0/Droidective-v3.2.0.dmg" sparkle:edSignature="00GFj1DXzXpJjcldDx+7l0XSnHZdODPsSFAIin/tfuNdTC/cqL0b5FWZmPWoIVYrM+8/C0YiJ8Q28L1RbkmxDA==" length="52369326" type="application/octet-stream"/>
    </item>
  </channel>
</rss>
