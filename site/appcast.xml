<?xml version="1.0" encoding="utf-8"?>
<rss version="2.0" xmlns:sparkle="http://www.andymatuschak.org/xml-namespaces/sparkle" xmlns:dc="http://purl.org/dc/elements/1.1/">
  <channel>
    <title>Droidective</title>
    <link>https://droidective.com/appcast.xml</link>
    <description>Most recent Droidective updates.</description>
    <language>en</language>
    <item>
      <title>Droidective 3.9.1</title>
      <sparkle:version>807</sparkle:version>
      <sparkle:shortVersionString>3.9.1</sparkle:shortVersionString>
      <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
      <description><![CDATA[
<style>
:root { color-scheme: light dark; }
body { font: 13px/1.55 -apple-system, BlinkMacSystemFont, sans-serif; margin: 0; }
h2 { font-size: 1.3em; margin: .2em 0 .4em; }
h3 { font-size: 1.05em; margin: 1.1em 0 .3em; }
p { margin: .5em 0; }
ul { margin: .4em 0; padding-left: 1.4em; }
li { margin: .3em 0; }
code { font-family: ui-monospace, SFMono-Regular, Menlo, monospace; font-size: .9em; background: color-mix(in srgb, currentColor 12%, transparent); padding: .1em .35em; border-radius: 4px; }
pre { background: color-mix(in srgb, currentColor 8%, transparent); padding: .6em .8em; border-radius: 6px; overflow-x: auto; }
pre code { background: none; padding: 0; }
a { color: #2f9e44; }
</style>
<p>A bug-fix release. The pop-out mirror window from 3.9.0 could hang the app —
including at launch, before anything was open — and Reactotron's timeline showed
stringified payloads as a wall of escaped text instead of the object they are.
A mirror screenshot can also be copied straight to the clipboard now.</p>
<h3>Mirror</h3>
<ul>
<li>
<strong>A pop-out mirror window no longer hangs the app.</strong> The window re-registered
itself on every view update, and each registration wrote state the app's own
window list reads — so the write ran the update and the update ran the write.
The app beach-balled for as long as one of those windows was open, with no way
out. Registration now happens once per window.</li>
<li>
<strong>Those windows are no longer restored at launch.</strong> macOS was reopening them
from its own saved state before the app had a workspace to own them, which
started a mirror session on a device nobody had asked about and made the hang
reproducible from launch — and immune to reinstalling the app, since saved
state lives outside the bundle.</li>
<li>
<strong>A mirror screenshot can be copied to the clipboard.</strong> The capture sheet on
Mirror Screen and the Mirror Wall offered Discard / Save / Edit, so getting the
image into a chat or a ticket meant opening the editor first. Copy sits beside
them and leaves the sheet up, so a copy can still be followed by Save or Edit.</li>
</ul>
<h3>Reactotron</h3>
<ul>
<li>
<strong>A stringified payload shows as an object.</strong> An API request's <code>data</code> field
arrives as the app's own <code>JSON.stringify</code> output, so an expanded event showed
escaped text where the body should be. It now renders as a real tree, with a
raw toggle per row for the payloads that only read in their escaped form. A
logged string that is itself JSON gets the same treatment.</li>
<li>
<strong>Find agrees with what the tree shows.</strong> Searching <code>storeId</code> in a payload
rendered as an object returned the whole 4 KB escaped blob on the <code>data</code> row
instead of the leaf two rows below it. The search now walks a string's parsed
form, and clicking a result reveals the row it actually lives on.</li>
<li>
<strong>A long API URL opens in place instead of over the rows below it.</strong> A signed
URL several hundred characters long drew all of its lines across the status
rows and the tab strip, because the layout had reserved three. The line is now
cut to the measured width and a click opens it whole at its real height.</li>
<li>
<strong>A parsed payload stays with the tab it belongs to.</strong> An API event's four tabs
are one tree, so switching tabs could render the previous tab's object on this
tab's row, under a chevron that claimed to be open.</li>
</ul>
<h3>APK signing</h3>
<ul>
<li>
<strong>A keystore password file that can't be written says why.</strong> Signing reported
only "Couldn't write the keystore password file"; the failure now carries the
path and the underlying reason, and creating the file is reported separately
from restricting it to its owner.</li>
</ul>
]]></description>
      <pubDate>Wed, 19 Aug 2026 12:31:28 +0000</pubDate>
      <enclosure url="https://github.com/Droidective/Droidective/releases/download/v3.9.1/Droidective-v3.9.1.dmg" sparkle:edSignature="zhGbgzG9wc3WtkBIVJAozOiU+huNYvLXMYIPlohZ9IoM8qcBa3flI99s36ml8k+aVJwmvJRPnXixtEHUBzy3CA==" length="144935779" type="application/octet-stream" />
    </item>
    
  </channel>
</rss>
