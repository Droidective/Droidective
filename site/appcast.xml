<?xml version="1.0" encoding="utf-8"?>
<rss version="2.0" xmlns:sparkle="http://www.andymatuschak.org/xml-namespaces/sparkle" xmlns:dc="http://purl.org/dc/elements/1.1/">
  <channel>
    <title>Droidective</title>
    <link>https://droidective.com/appcast.xml</link>
    <description>Most recent Droidective updates.</description>
    <language>en</language>
    <item>
      <title>Droidective 3.10.0-beta.3</title>
      <sparkle:channel>beta</sparkle:channel>
      <sparkle:version>889</sparkle:version>
      <sparkle:shortVersionString>3.10.0-beta.3</sparkle:shortVersionString>
      <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
      <description><![CDATA[
<style>
:root { color-scheme: light dark; }
body { font: 13px/1.55 -apple-system, BlinkMacSystemFont, sans-serif; margin: 0; }
h2 { font-size: 1.3em; margin: .2em 0 .4em; }
h3 { font-size: 1.05em; margin: 1.1em 0 .3em; }
p { margin: .5em 0; }
ul { margin: .4em 0; padding-left: 1.4em; }
li { margin: .3em 0; }
code { font-family: ui-monospace, SFMono-Regular, Menlo, monospace; font-size: .9em; background: color-mix(in srgb, currentColor 12%, transparent); padding: .1em .35em; border-radius: 4px; }
pre { background: color-mix(in srgb, currentColor 8%, transparent); padding: .6em .8em; border-radius: 6px; overflow-x: auto; }
pre code { background: none; padding: 0; }
a { color: #2f9e44; }
</style>
<p>The Windows and Linux app grows the three things it was missing that people
notice first: it tells you when something finishes while you are elsewhere, it
stays out of the way without quitting, and it can be summoned over whatever you
are working in. Behind all of that, the reason the Windows app looked broken on
its first launch turned out not to be what the note in the tracker said.</p>
<h3>The Windows app was hanging, not being slow</h3>
<ul>
<li>
<strong>The first <code>adb devices</code> on Windows never returned.</strong> The app came up
showing "0 features" and no error, which is exactly what Linux did before the
last beta fixed it there. It had been written down as <em>slow rather than dead</em>
on the evidence of a stand-in that returned after thirty seconds — but the
stand-in slept, and adb's grandchild is a <em>server</em>, which does not exit at
all. Timing the real call settled it.</li>
<li>
<strong>The fix is a second way to notice a child has gone.</strong> Windows now waits on
the process handle, which the OS signals the moment the child exits —
inherited pipes and surviving grandchildren included. The whole ADBKit suite
runs there in six seconds where one test used to sit at thirty, and the
regression test that was gated off Windows for "destabilising" a neighbour
runs again: that neighbour was the same missing exit report, seen from the
other end.</li>
<li>
<strong>Both smoke tests now time it.</strong> The Linux one, on every pull request, and
the Windows one on every beta tag, ask the running app for its device list
and fail if the answer takes longer than ten seconds. An empty answer is
fine; an answer arriving is the point.</li>
<li>
<strong>The app no longer waits for the device list to show anything.</strong> Its two
startup calls used to arrive together, which handed the slower of them the
decision about when the app became usable. The sidebar now paints as soon as
the registry answers, whatever adb is doing.</li>
</ul>
<h3>It tells you when you are looking somewhere else</h3>
<ul>
<li>
<strong>A result that lands while you are in another app posts a notification.</strong>
Not a list of events, but the same rule the Mac uses: the results already
worth keeping in the notification panel are the ones worth interrupting for,
so an install finishing and a watched crash landing both arrive without
either screen knowing a tray exists. Failures carry a sound; routine
confirmations stay quiet.</li>
</ul>
<h3>It stays running without staying in the way</h3>
<ul>
<li>
<strong>Closing the window leaves Droidective in the tray</strong> instead of quitting it,
stops the work that was only running because a window was open — terminal
shells included — and keeps the shortcuts alive. The tray menu is the Mac's
menu-bar menu: the selected device, Quick Actions, Screenshot, Mirror Screen,
the features you chose in Settings, then Open and Quit.</li>
<li>
<strong>Only where there is somewhere to click.</strong> A Linux session can decline to
give an app a tray icon, and hiding a window nobody can bring back is not a
mode worth having, so the app checks and Settings says so instead of
offering the switch. The <code>.deb</code> now depends on <code>libayatana-appindicator3-1</code>.</li>
<li>
<strong>Recorded shortcuts are registered with the system.</strong> They fire from
whatever app you are in, and from a window closed into the tray. A
combination another app already holds is refused by the platform and goes on
working while Droidective has focus, rather than working nowhere.</li>
</ul>
<h3>Quick Actions</h3>
<ul>
<li>
<strong>A panel over whatever you are doing</strong>, on a shortcut you record in
Settings ▸ Hotkeys or from the tray's first item: a search field over a grid
of everything runnable in place, your saved commands under it, and the
full-app screens under those. Arrows move the grid while you are still
typing, Enter runs, Escape closes.</li>
<li>
<strong>It asks before it acts.</strong> A destructive action waits for a second Enter. A
device-scoped one asks which device when more than one is connected, with an
All devices row for the actions that fan out. A form action shows its fields;
so does a toggle, because guessing a direction and writing it to a device is
worse than asking.</li>
<li>Manage Apps, Emulators and Install APK are not in the panel yet.</li>
</ul>
<h3>Appearance</h3>
<ul>
<li>
<strong>Pick the window's background and text colour.</strong> Eight presets, a colour
well and a hex field, with the lifted surfaces, the hairlines and the muted
text derived from what you picked — so a custom colour keeps the same
hierarchy the stock one has. A light background switches the whole app to
the light treatment, and the Theme picker says so rather than fighting it.</li>
</ul>
]]></description>
      <pubDate>Sun, 30 Aug 2026 21:44:26 +0000</pubDate>
      <enclosure url="https://github.com/Droidective/Droidective/releases/download/v3.10.0-beta.3/Droidective-v3.10.0-beta.3.dmg" sparkle:edSignature="tfR+G7sqCYiarkD6vhUz1oDv7hTBh7rwesqk2Dm5AwWncKWJ/ULf0oVLPCFsJk+/2iXKYGD2o3X/9kshCVlNDg==" length="145189856" type="application/octet-stream" />
    </item>
    <item>
      <title>Droidective 3.9.2</title>
      <sparkle:version>830</sparkle:version>
      <sparkle:shortVersionString>3.9.2</sparkle:shortVersionString>
      <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
      <description><![CDATA[
<style>
:root { color-scheme: light dark; }
body { font: 13px/1.55 -apple-system, BlinkMacSystemFont, sans-serif; margin: 0; }
h2 { font-size: 1.3em; margin: .2em 0 .4em; }
h3 { font-size: 1.05em; margin: 1.1em 0 .3em; }
p { margin: .5em 0; }
ul { margin: .4em 0; padding-left: 1.4em; }
li { margin: .3em 0; }
code { font-family: ui-monospace, SFMono-Regular, Menlo, monospace; font-size: .9em; background: color-mix(in srgb, currentColor 12%, transparent); padding: .1em .35em; border-radius: 4px; }
pre { background: color-mix(in srgb, currentColor 8%, transparent); padding: .6em .8em; border-radius: 6px; overflow-x: auto; }
pre code { background: none; padding: 0; }
a { color: #2f9e44; }
</style>
<p>Pulling events out of a busy log. Rows in the Reactotron timeline and the JS
Console can be selected — ⌘-click one, ⇧-click a range, drag across them — and
copied whole, payload included. An API call's status code also reads off its
collapsed row, and the empty "Screen Mirror" window that could appear at launch
is gone.</p>
<h3>Reactotron and JS Console</h3>
<ul>
<li>
<strong>Select rows and copy them whole.</strong> Pulling a few events out of a busy
timeline meant right-clicking them one at a time. ⌘-click toggles a row,
⇧-click takes the range from the last one picked, and dragging across rows
sweeps a range; the toolbar shows how many are picked, with a copy menu, and
⌘C copies them. A copy carries the whole event rather than the line already on
screen — the row's summary followed by the complete payload: an API call's
request body, headers and response, an action's or saga's arguments. Copy as
JSON gives the same events in the export's wire form.</li>
<li>
<strong>A selection can't copy what the feed no longer has.</strong> Rows that were
cleared, filtered out, or trimmed while the selection stood are dropped from
it, so a copy never contains events nobody can see.</li>
<li>
<strong>The gestures stay out of each other's way.</strong> Only a row's header carries the
drag, so dragging through an expanded payload still selects that text, and ⌘C
is bound only while rows are picked — it never shadows copying text out of the
search field or an open payload.</li>
</ul>
<h3>Reactotron</h3>
<ul>
<li>
<strong>An API event's status code sits on its collapsed row.</strong> Reading a timeline is
mostly looking for the one call that didn't return 200, and that meant
expanding rows one at a time. The code now sits between the API badge and the
method — green for 2xx, orange for a client error, red for a server error,
neutral for a redirect. Only the code is tinted, so the timeline's own badge
colours still read. A request that never got a response reports status 0 on the
wire, so it reads <code>ERR</code>.</li>
</ul>
<h3>Mirror</h3>
<ul>
<li>
<strong>No empty "Screen Mirror" window at launch.</strong> A 420×850 window with a title
bar and nothing in it could appear on its own at launch — and in the worst case
it was the only window the app had. The pop-out mirror's window group persists
the device it was presented with and re-presents it at the next launch, before
any workspace exists for it to attach to, so it rendered nothing and no main
window was created either. A pop-out now has to have been asked for in this
session; anything else closes itself once a real window is up.</li>
</ul>
]]></description>
      <pubDate>Tue, 25 Aug 2026 12:15:04 +0000</pubDate>
      <enclosure url="https://github.com/Droidective/Droidective/releases/download/v3.9.2/Droidective-v3.9.2.dmg" sparkle:edSignature="9J2wA5//XpPHq08jgJerK9+IaIjjFOWN3+WYCH2sjO9DDOtLfcAbjhNFbDU/K4VORPico+VsbjbVUI5Kj4uxAw==" length="145155632" type="application/octet-stream"/>
    </item>
  </channel>
</rss>
