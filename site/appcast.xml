<?xml version="1.0" encoding="utf-8"?>
<rss version="2.0" xmlns:sparkle="http://www.andymatuschak.org/xml-namespaces/sparkle" xmlns:dc="http://purl.org/dc/elements/1.1/">
  <channel>
    <title>Droidective</title>
    <link>https://droidective.com/appcast.xml</link>
    <description>Most recent Droidective updates.</description>
    <language>en</language>
    <item>
      <title>Droidective 3.7.1</title>
      <sparkle:version>625</sparkle:version>
      <sparkle:shortVersionString>3.7.1</sparkle:shortVersionString>
      <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
      <description><![CDATA[
<style>
:root { color-scheme: light dark; }
body { font: 13px/1.55 -apple-system, BlinkMacSystemFont, sans-serif; margin: 0; }
h2 { font-size: 1.3em; margin: .2em 0 .4em; }
h3 { font-size: 1.05em; margin: 1.1em 0 .3em; }
p { margin: .5em 0; }
ul { margin: .4em 0; padding-left: 1.4em; }
li { margin: .3em 0; }
code { font-family: ui-monospace, SFMono-Regular, Menlo, monospace; font-size: .9em; background: color-mix(in srgb, currentColor 12%, transparent); padding: .1em .35em; border-radius: 4px; }
pre { background: color-mix(in srgb, currentColor 8%, transparent); padding: .6em .8em; border-radius: 6px; overflow-x: auto; }
pre code { background: none; padding: 0; }
a { color: #2f9e44; }
</style>
<p>A new Developer Settings panel drives Android's Developer Options over adb,
the mirror gains a Show touches option, emulators wipe and relaunch as
separate actions, and the Terminal's collapsed rail keeps every tab
reachable — with dropped files typing their paths into the shell. Plus
fixes for a welcome-tour crash, an update pill that could spin forever, and
JS Console hangs on chatty Metro streams.</p>
<h3>Developer Settings — the 59th feature</h3>
<ul>
<li>
<strong>Android's Developer Options over adb</strong> — show taps, pointer location,
layout bounds, GPU overdraw, GPU profile bars, strict-mode flash, don't
keep activities, and the three animation scales, all in one panel. Values
load from the device so the panel shows ground truth, and overlay toggles
repaint running apps immediately — no app restart.</li>
</ul>
<h3>Terminal</h3>
<ul>
<li>
<strong>Collapsed rail keeps tabs reachable</strong> — the thin rail now lists every
open shell as a two-letter badge: click to switch, hover for the full
name, right-click for the tab menu.</li>
<li>
<strong>Dropping files types their paths</strong> — drop Finder files on a shell and
their paths are typed in (quoted only when needed, trailing space), with
that pane focused — like dropping onto Terminal.app.</li>
</ul>
<h3>Screen Mirror</h3>
<ul>
<li>
<strong>Show touches</strong> — a new ⋯ options menu (audio lives there too) toggles
Android's touch dot for the session: it flips instantly, works
mid-recording, and the device's own setting is restored when the session
ends. The dot draws only for physical touches on the device — taps
injected by clicking the mirror don't render it.</li>
</ul>
<h3>Emulators</h3>
<ul>
<li>
<strong>Wipe without launching</strong> — "Wipe Data…" on a stopped AVD wipes in place
(user data, caches, snapshots — the same set Android Studio removes)
instead of booting the emulator to do it.</li>
<li>
<strong>Relaunch</strong> — running AVDs gain a Relaunch button: graceful stop, wait
for the console port to free, boot the same AVD.</li>
</ul>
<h3>Fixes</h3>
<ul>
<li>
<strong>Welcome tour crash</strong> — double-activating Next or Back (Return
auto-repeat, or a double-click racing the render) could step past the
last page and crash mid-onboarding. Paging is now clamped to the range.</li>
<li>
<strong>Update pill no longer sticks</strong> — cancelling the quit confirmation or an
interrupted install left the sidebar pill on "Installing update…"
forever; it now returns to a clickable "Relaunch to update" (or a
retryable error state).</li>
<li>
<strong>JS Console hangs on chatty streams</strong> — a busy Metro feed drove up to 60
full feed re-renders a second and re-ran link detection on every visible
row; flushes are now paced and link detection cached, ending the app
hangs reported with the console open.</li>
</ul>
]]></description>
      <pubDate>Fri, 24 Jul 2026 11:18:16 +0000</pubDate>
      <enclosure url="https://github.com/Droidective/Droidective/releases/download/v3.7.1/Droidective-v3.7.1.dmg" sparkle:edSignature="UDKgaVuFDqqf5NQO62MA7ViO+t6DMln0qwEkgf5l/b55a05otoG4dkDwk+h3vwFbIzcfBdj0+kdJPn3ZFJKLBQ==" length="141267719" type="application/octet-stream" />
    </item>
    
  </channel>
</rss>
