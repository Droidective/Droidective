<?xml version="1.0" encoding="utf-8"?>
<rss version="2.0" xmlns:sparkle="http://www.andymatuschak.org/xml-namespaces/sparkle" xmlns:dc="http://purl.org/dc/elements/1.1/">
  <channel>
    <title>Droidective</title>
    <link>https://droidective.com/appcast.xml</link>
    <description>Most recent Droidective updates.</description>
    <language>en</language>
    <item>
      <title>Droidective 3.10.0</title>
      <sparkle:version>923</sparkle:version>
      <sparkle:shortVersionString>3.10.0</sparkle:shortVersionString>
      <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
      <description><![CDATA[
<style>
:root { color-scheme: light dark; }
body { font: 13px/1.55 -apple-system, BlinkMacSystemFont, sans-serif; margin: 0; }
h2 { font-size: 1.3em; margin: .2em 0 .4em; }
h3 { font-size: 1.05em; margin: 1.1em 0 .3em; }
p { margin: .5em 0; }
ul { margin: .4em 0; padding-left: 1.4em; }
li { margin: .3em 0; }
code { font-family: ui-monospace, SFMono-Regular, Menlo, monospace; font-size: .9em; background: color-mix(in srgb, currentColor 12%, transparent); padding: .1em .35em; border-radius: 4px; }
pre { background: color-mix(in srgb, currentColor 8%, transparent); padding: .6em .8em; border-radius: 6px; overflow-x: auto; }
pre code { background: none; padding: 0; }
a { color: #2f9e44; }
</style>
<p>Four fixes to what the app does while you are not watching it, and a wider
video editor. The Windows and Linux app that most of this cycle went into is on
the beta channel; this release is macOS.</p>
<h3>The app is quieter in the background</h3>
<ul>
<li>
<strong>The Reactotron timeline was redrawing 62 times a second</strong>, whether or not
anyone was looking at it. Every open tab stays mounted, so that cost was paid
across every feed you had open, not just the one on screen — with nine or more
tabs open it was the app's largest source of multi-second stalls. It now
redraws four times a second while you are in the app and once a second while
you are in another one, which is what the JS Console and Logcat already did.</li>
<li>
<strong>Failed HTTP requests are no longer reported as app errors.</strong> Metro answers
500 to any stack frame it cannot map, which is routine and already handled;
each one was being filed as a Droidective error. Requests you send yourself
from API Testing were being captured the same way, with their URLs — nothing
in the app should send those anywhere, and now nothing does.</li>
</ul>
<h3>Reactotron reconnects after a restart</h3>
<ul>
<li>
<strong>Restarting the app you are debugging re-opens its <code>adb reverse</code> tunnel.</strong>
The tunnel is what lets the app on the device find Droidective, and the
restart button did not touch it — so a restarted app could come up unable to
connect, with nothing on screen saying why.</li>
<li>
<strong>A tunnel that dies with the device still connected now recovers.</strong>
Replugging was already handled; an <code>adb kill-server</code> or a USB re-enumeration
that kept the same serial was not.</li>
</ul>
<h3>The video editor opens what ffmpeg reads</h3>
<ul>
<li>
<strong>Fifteen container formats</strong> — mkv, webm, avi, flv, ts, m2ts, mpg, wmv, 3gp,
ogv and more, alongside mp4, mov and m4v. The editor exports through ffmpeg
and always could read these; only the open panel disagreed, so <code>.mkv</code> was
greyed out in a file picker for a file the app could edit.</li>
<li>
<strong>macOS cannot play most of them</strong>, so anything it refuses is converted to a
playable copy in the background — the video appears, trimming works, and the
export still runs from your original file, so nothing loses quality.</li>
<li>
<strong>Videos open from Finder.</strong> Right-click ▸ Open With ▸ Droidective, for every
format above. Your existing default player keeps every file type it had.</li>
</ul>
<h3>Notifications and progress</h3>
<ul>
<li>
<strong>Notifications now arrive while the app is in front.</strong> Anything that reaches
the in-app notification bar — a caught crash, a finished install, a saved
recording, an available update — also appears in Notification Center, and
clicking one opens the bar on the matching entry. Previously they were posted
only when the app was hidden, so a five-second toast was the only trace an
event left.</li>
<li>
<strong>Relaunching an emulator says what it is doing.</strong> It waits for the emulator's
console port to free before booting again, which takes up to twenty seconds,
and reported nothing for the whole wait. The row now shows the stage; a
failed stop says what adb said instead of booting a second copy on top.</li>
</ul>
]]></description>
      <pubDate>Fri, 04 Sep 2026 05:36:10 +0000</pubDate>
      <enclosure url="https://github.com/Droidective/Droidective/releases/download/v3.10.0/Droidective-v3.10.0.dmg" sparkle:edSignature="Ch5czlGKWF3+hFCzTc2wi03ZfXF87aH8AL28+IsXvmldjs0QXrrPffP/DMqtZi1vdpvRAvi7AMrwMXgLlen8BA==" length="145384627" type="application/octet-stream" />
    </item>
    
  </channel>
</rss>
