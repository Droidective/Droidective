<?xml version="1.0" encoding="utf-8"?>
<rss version="2.0" xmlns:sparkle="http://www.andymatuschak.org/xml-namespaces/sparkle" xmlns:dc="http://purl.org/dc/elements/1.1/">
  <channel>
    <title>Droidective</title>
    <link>https://droidective.com/appcast.xml</link>
    <description>Most recent Droidective updates.</description>
    <language>en</language>
    <item>
      <title>Droidective 3.3.1</title>
      <sparkle:version>456</sparkle:version>
      <sparkle:shortVersionString>3.3.1</sparkle:shortVersionString>
      <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
      <description><![CDATA[
<style>
:root { color-scheme: light dark; }
body { font: 13px/1.55 -apple-system, BlinkMacSystemFont, sans-serif; margin: 0; }
h2 { font-size: 1.3em; margin: .2em 0 .4em; }
h3 { font-size: 1.05em; margin: 1.1em 0 .3em; }
p { margin: .5em 0; }
ul { margin: .4em 0; padding-left: 1.4em; }
li { margin: .3em 0; }
code { font-family: ui-monospace, SFMono-Regular, Menlo, monospace; font-size: .9em; background: color-mix(in srgb, currentColor 12%, transparent); padding: .1em .35em; border-radius: 4px; }
pre { background: color-mix(in srgb, currentColor 8%, transparent); padding: .6em .8em; border-radius: 6px; overflow-x: auto; }
pre code { background: none; padding: 0; }
a { color: #2f9e44; }
</style>
<p>iOS Logs is rebuilt around how the simulator's unified log actually behaves,
and ⌘F now always lands where you expect it.</p>
<h3>iOS Logs, rebuilt</h3>
<ul>
<li>
<strong>Scoped to your apps by default</strong> — the unified log unfiltered is the whole
simulator OS (thousands of lines a second). The stream now covers only
installed apps' processes; <em>Everything</em> is one click away when you need
system logs.</li>
<li>
<strong>Scrollback that holds still</strong> — scrolling up freezes the feed; new lines
wait behind a "N new" pill (with a waiting count in the status bar) and
fold in when you click it or scroll back to the tail. No more unscrollable
firehose.</li>
<li>
<strong>Xcode-style entries</strong> — the message leads, with a toggleable metadata
line (time · process (pid) · subsystem · category), a severity color bar,
and tinted error/fault rows.</li>
<li>
<strong>Error &amp; fault counters</strong> — live tallies in the status bar; click to flip
the feed to errors only and back.</li>
<li>
<strong>Sharper filtering</strong> — a multi-select level menu (Info and Debug also
widen what the simulator emits), a process menu built from the stream
itself (or right-click a line), free-text filter, and ⌘F find.</li>
</ul>
<h3>⌘F lands where you expect</h3>
<ul>
<li>
<strong>No more focus jumping to the sidebar</strong> — ⌘F used to be claimable by a
hidden tab, sending focus to the sidebar search. It now always targets the
screen you're on: the find bar in Logcat, iOS Logs, and the JS Console —
and in Reactotron it focuses the timeline search, its only text filter.
The sidebar search only takes focus from ⌘T or a click.</li>
</ul>
<h3>Settings</h3>
<ul>
<li>
<strong>Check for Updates in Settings</strong> — the Updates section shows the installed
version with a Check for Updates button, alongside the update toggles.</li>
<li>
<strong>Quick Actions can close after a run</strong> — a new option dismisses the panel
once an action succeeds (failures keep it open so the error stays
readable).</li>
</ul>
]]></description>
      <pubDate>Wed, 15 Jul 2026 14:25:08 +0000</pubDate>
      <enclosure url="https://github.com/Droidective/Droidective/releases/download/v3.3.1/Droidective-v3.3.1.dmg" sparkle:edSignature="ttMz6WMZzizyLFaeF6POK/lKY8rPtQnilRLrklkWXM7G20jdemL8vdqLPeRNrEDlykjgTSd3hqSDJajcuiv/Bg==" length="52822002" type="application/octet-stream" />
    </item>
    
  </channel>
</rss>
