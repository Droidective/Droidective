{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The app's two windows — the main one and the Quick Actions panel. Deliberately minimal: neither webview gets shell, filesystem, or HTTP permission of its own — every daemon call goes through a Rust command, so the daemon's bearer token never enters a webview and a compromised page cannot reach 127.0.0.1 directly.",
  "windows": ["main", "quick"],
  "permissions": ["core:default"]
}
